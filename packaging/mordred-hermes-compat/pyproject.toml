[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[project]
name = "mordred-hermes"
version = "0.1.0a17"
description = "Compatibility shim for the renamed hermes-mordred distribution"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "InternetMaximalism" }]
license = { text = "MIT" }
dependencies = ["hermes-mordred==0.1.0a17"]

[project.optional-dependencies]
keyvault = ["hermes-mordred[keyvault]==0.1.0a17"]
extension = ["hermes-mordred[extension]==0.1.0a17"]
macos = ["hermes-mordred[macos]==0.1.0a17"]
ethereum = ["hermes-mordred[ethereum]==0.1.0a17"]
messaging = ["hermes-mordred[messaging]==0.1.0a17"]
tor-control = ["hermes-mordred[tor-control]==0.1.0a17"]
integration = ["hermes-mordred[integration]==0.1.0a17"]
dev = ["hermes-mordred[dev]==0.1.0a17"]

[project.urls]
Documentation = "https://github.com/InternetMaximalism/hermes-mordred/tree/main/docs"
Source = "https://github.com/InternetMaximalism/hermes-mordred"

# The compatibility distribution owns metadata only. Runtime modules and the
# hermes-mordred console script belong exclusively to the new distribution so
# uninstalling either project cannot delete files owned by the other one.
[tool.hatch.build.targets.wheel]
bypass-selection = true

[tool.hatch.build.targets.sdist]
include = ["/README.md", "/pyproject.toml"]
