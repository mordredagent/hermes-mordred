# Mordred — TODO (Hermes-base)

> **Note**: This TODO is the Mordred implementation checklist on the `Hermes (NousResearch/hermes-agent)` base. The old OpenClaw-based version remains at `../../mordred/mordred-mvp-docs/TODO.md` (deprecated).
>
> The `[x]` completion markers in the old TODO reflect work history under the OpenClaw environment; with the migration to Hermes, **all of them are reopened as pending**. Some concepts (e.g., the 5-plugin scaffold) are reusable, but file paths, the test runner, etc. need a full rewrite.

Actionable checklist derived from `docs/SPEC.md` and `docs/PLAN.md`.
Each item is a developer-pickable task. Cross-references point back to SPEC/PLAN sections rather than restating context.
Update this file whenever a task is completed (check the box) or when SPEC/PLAN evolves (add/remove tasks to match).
Phases are sequenced; do not start Phase N+1 until the Phase N acceptance gate is green.
Open decisions are surfaced as `DECIDE:` items at the top of the relevant phase.

**Plugin-Only Architecture policy (zero-PR, finalized 2026-05-07)**: Nearly all work lands in `src/mordred_hermes/<plugin>/`. In v1 we submit no PRs whatsoever to Hermes upstream (MIGRATION.md §10 row 4). Defense against the disable footgun is handled entirely by the plugin-side **strict-mode startup refusal** (Phase 1.1 H3 task, SPEC.md §Plugin-disable protection Tier A) and the `mordred.degraded.disable_unprotected` audit log. Items that truly need hard-enforcement are escalated in v2 to the vendored fork extra (`mordred-hermes[hard-lock]`, Tier B, UPSTREAM.md §Tier B).

## Standalone-repo repair backlog (2026-07-01)

The split from `Mordred-Hermes-monorepo` (2026-06-30/07-01) left `.github/workflows/` missing entirely. See also CI.md §Standalone-repo adaptations and ROADMAP.md §Remaining browser-extension gateway integration.

- [x] Restore the `test` job (lint + mypy strict + pytest matrix) in `.github/workflows/ci.yml`
- [x] Restore `.github/workflows/upstream-check.yml` (simplified `git clone` to `pip install hermes-agent`, since it's now published on PyPI)
- [x] Restore `.github/workflows/labeler.yml` + `.github/labeler.yml`
- [x] Fix the `test_provider_identity.py::test_replica_matches_hermes_source` drift (resync the 6 aliases hermes-agent 0.14.0 added)
- [x] Add `eth-account`/`rlp` to the `ethereum` extra and add a new `messaging` extra (`qrcode`) in `pyproject.toml` (both were imported at call time but never declared)
- [x] Make `hermes mordred extension pair` fail closed when no pairing backend is available (exit code 2 + clear message instead of a raw `ImportError`). Superseded for current installs by the packaged `mordred_hermes.extension.pairing` backend; the failure remains for incomplete/pre-port builds
- [x] Add `HERMES_HOME` to the `ci.yml` `pytest` step's env (matches README.md's documented canonical test invocation)
- [x] Fix `docs/dev/CI.md`'s "Auditing" section (expected-output list and repo-name casing) to match the 3 workflows actually restored, and mark the upstream-origin-workflows paragraph as monorepo-era/not applicable here
- [x] Restore `.github/workflows/release.yml` (PyPI Trusted Publishing, M7) — workflow restored 2026-07-06 (paths adapted to the flat repo root); the one-time manual operator setup (pending publishers + GitHub Environments, CI.md §Initial setup) was completed 2026-07-07 together with the M7 name reservation (§0.5 M7)
- [x] Restore `.github/workflows/integration-vpn.yml` — restored 2026-07-06 (`workflow_dispatch`-only); dispatching still requires the paid-account `MORDRED_MULLVAD_ACCOUNT` repo secret
- [x] Restore the `integration-tor` / `tpmkey-helper` / `tpmkey-helper-tpm` jobs in `ci.yml` — restored 2026-07-06; `native/**` added to the paths filter (Docker / Rust / swtpm all provisioned on the ubuntu-24.04 runner inside the jobs)
- [x] Port the browser-extension WebSocket server, pairing/crypto/chat/RPC/history modules, and tests into `mordred_hermes.extension`; add the foreground `extension serve` launcher (#30/#32, 2026-07-10)
- [ ] Decide automatic lifecycle integration for the packaged extension server (upstream plugin boot hook vs. launchd/systemd documentation vs. permanently explicit `extension serve`) — see ROADMAP.md §Remaining browser-extension gateway integration

---

## Phase 0 — Operational Setup (blocks all later phases)

### Open decisions

- [x] ~~DECIDE: whether to introduce `.github/workflows/upstream-check.yml` (Hermes hook **name** drift detection; payload field shape is v2) in v1, or defer it to v2~~ → **confirmed for v1** (2026-05-09, PR #8, weekly cron Monday 03:00 UTC; on drift, auto-files an issue labeled `actionable` + `upstream-drift`)

### 0.1 Confirm repo & venv

- [x] Sanity-check in `Mordred-Hermes/` (that Hermes itself runs) — **completed 2026-05-16**. The console script is `hermes --version` (`python -m hermes_cli` doesn't work — no `__main__`). The `hermes-mordred --help` subcommand tree was also verified working
- [x] Activate the venv: `source .venv/bin/activate` (matches the probe order in Hermes's `scripts/run_tests.sh`) — **verified 2026-05-16**. `mordred_hermes` is importable from `.venv` via editable install
- [x] Create the `~/.hermes/` profile: run `hermes setup` to finish Hermes configuration — **verified 2026-05-16**. `~/.hermes/config.yaml` + `auth.json` generated

### 0.2 Hermes upstream tracking strategy (optional, rebase not recommended)

- [x] (Optional) `git remote add hermes-upstream https://github.com/NousResearch/hermes-agent.git` — **2026-05-16 decision: not added in v1**. Mordred-Hermes is a plugin-development repository, not a fork of upstream, so rebasing is unnecessary. Can be added any time later following the `UPSTREAM.md` §Optional remote procedure if needed
- [x] Record the Hermes upstream strategy decision in `docs/UPSTREAM.md` (rewritten in Phase D) — **done**. The actual file is `docs/dev/UPSTREAM.md` (114 lines): records the repository position / zero-PR commitment / optional remote procedure / hook signature drift detection

### 0.3 Reserve Mordred-owned filesystem paths (kept in sync with PATHS.md)

- [x] `~/.hermes/mordred/audit.log` documented in `docs/PATHS.md` with Phase 1 as owner (done, part of Phase C)
- [x] `~/.hermes/mordred/policy.json` (writer = `mordred_wizard`, reader = privacy_check/llm_guard/network) documented in `docs/PATHS.md` (done)
- [x] `~/.hermes/mordred/credentials/` documented in `docs/PATHS.md` with Phase 3 as owner (done)
- [x] `~/.hermes/mordred/keyvault/` documented in `docs/PATHS.md` with Phase 4 as owner (done)
- [x] Each plugin's `README.md` (created in 0.4) cross-references its own path

### 0.4 Plugin scaffolding (five plugins)

- [x] Scaffold `src/mordred_hermes/privacy_check/`: `plugin.yaml`, `__init__.py` (with `register(ctx)`), `policy.py`, `skill_frontmatter.py`, `audit.py`, `install_wrapper.py`, `README.md`, `tests/test_*.py`
- [x] Scaffold `src/mordred_hermes/wizard/`: `plugin.yaml`, `__init__.py`, `cli.py`, `configure.py`, `upgrade.py`, `policy_writer.py`, `policy_explainer.py`, `README.md`, `tests/test_*.py`
- [x] Scaffold `src/mordred_hermes/llm_guard/`: `plugin.yaml` (`privacy_lock: true`), `__init__.py`, `local_adapter.py`, `transport.py`, `health.py`, `override.py`, `harness_detect.py`, `README.md`, `tests/test_*.py` (`transport.py` and `override.py` were later removed when their v1 integration points proved unavailable; see §2.1 H1/B2)
- [x] Scaffold `src/mordred_hermes/network/`: `plugin.yaml`, `__init__.py`, `paths/{tor,vpn,clearnet}.py`, `proxy_env.py`, `provider_transport_flagger.py`, `api.py`, `runtime.py`, `README.md`, `tests/test_*.py`
- [x] Scaffold `src/mordred_hermes/keyvault/`: `plugin.yaml`, `__init__.py`, `native.py` (lazy import), `api.py`, `crypto.py`, `wrap.py`, `backup.py`, `recovery.py`, `digest.py`, `seed_display.py`, `network_fallback.py`, `log_encryption.py`, `README.md`, `tests/test_*.py` (macOS bridges remain in the `macos` extra; Linux TPM support later shipped through the packaged helper)
- [x] Define the initial Phase 0 `register(ctx)` stubs. These have since been replaced by the landed hook / CLI / provider / tool registrations from Phase 1.x onward (see Phase 1.1 / 1.3 / 2.1 / 3.1 / 4.1)
- [x] (Phase 1.0 prep) Narrow the typing of `ctx` to `hermes_cli.plugins.PluginContext` via `TYPE_CHECKING`, or substitute a Mordred-internal `Protocol`. Make it possible for mypy to catch Hermes hook payload drift — **completed 2026-05-10** (Phase 1.1 PR; each plugin adopted the narrow `Protocol` approach. See `src/mordred_hermes/privacy_check/_typing.py:PluginContext`; other plugins extend it as needed in Phase 2/3/4)
- [x] Declare `privacy_lock: true` in the five manifest-backed plugins as a declarative marker (zero-PR commitment; Hermes core ignores it). The runtime gate uses an explicit canonical list of all six entry points rather than discovering siblings from this field

### 0.5 `mordred-hermes` package scaffold

- [x] Create `pyproject.toml` (2026-05-09, with the deviations below):
  - `name = "mordred-hermes"`
  - `dynamic = ["version"]` + `[tool.hatch.version] path = "docs/VERSION"` (M6: make the VERSION file the single source of truth)
  - `requires-python = ">=3.10"`
  - `dependencies = ["hermes-agent>=1.0", "ruamel.yaml>=0.18"]` (H1: make hermes-agent mandatory at install time, fail-fast)
  - `[project.optional-dependencies]` with `macos = ["pyobjc-framework-Security>=10.0"]`
  - `[project.entry-points."hermes_agent.plugins"]` exposing `register` for the initial 5 plugins (now 6 with `mordred_e2e`)
  - `[project.metadata]` declaring `mordred-min-hermes-version` (for double-checking at runtime)
  - **Deviations from spec** (Phase 0.5 follow-ups required):
    - ~~Hardcoded `version = "0.1.0a0"` (dynamic version not used)~~ — **completed 2026-06-09**: removed the static `version` and migrated to `dynamic = ["version"]` + `[tool.hatch.version] path = "src/mordred_hermes/__about__.py"`. Placing the canonical source inside the importable package (`__about__.py`) avoids the build-isolation problem (the docs-tree `docs/dev/VERSION` is cross-directory and unreadable from sdist→wheel, which was the original concern behind this deviation). Verified with `uv build` that both sdist and wheel resolve to `0.1.0a0` (the sdist bundles `__about__.py`). Bumping is done by `tools/bump_version.py`, which updates `__about__.py` (canonical) / the docs `VERSION` / all `plugin.yaml` files together; `tests/test_packaging_versions.py` pins their agreement and that stub < real
    - `dependencies = ["hermes-agent>=0.11.0"]` (spec says `>=1.0`, but Hermes upstream's latest is 0.11.0). Raise the pin once Hermes reaches 1.0 GA
    - Since PEP 621 doesn't allow `[project.metadata]`, changed to a `[tool.mordred] min-hermes-version = "0.11.0"` table
    - The entry point specifies **the module only** (`mordred_hermes.privacy_check`, no `:register` suffix), matching the Hermes loader (`hermes_cli/plugins.py:_load_entrypoint_module` → `getattr(module, "register")`). Examples in SPEC/PLAN/TODO have been corrected accordingly
- [x] Confirmed `pip install -e ./mordred-hermes` succeeds as an editable install
- [x] Confirm `hermes plugins list` shows all 5 mordred_* entries — **deferred to the Phase 1.3 wizard to close the UX gap** (entry-point plugins are outside the scope of Hermes upstream's `_discover_all_plugins()`. The `hermes mordred plugins list` wrapper, provided by `wizard/plugins_list.py`, already achieves the same outcome as the L130 acceptance gate)
- [x] **M7: PyPI name reservation** — before making v1 docs public, claim the `mordred-hermes` name on TestPyPI / PyPI via a stub upload (empty `0.0.0.dev0`) to preempt supply-chain squatting attacks. **2026-05-18: reservation tooling implemented** — stub package (`packaging/name-reservation/`, `0.0.0.dev0`, verified `uv build` + `twine check` PASS) + `.github/workflows/release.yml` (PyPI Trusted Publishing OIDC, `mode=reserve`/`release` × `target=testpypi`/`pypi`, `workflow_dispatch`-only) + a version invariant test (`tests/test_packaging_versions.py`, pinning `0.0.0.dev0 < 0.1.0a0`). **Remaining**: the operator must manually register pending publishers on PyPI/TestPyPI → then run `release.yml` with `mode=reserve` against TestPyPI→PyPI (runbook = `CI.md` §`release.yml` details). Check off this box once the upload is complete. **Completed 2026-07-07**: pending-publisher registration (operator) + creation of the `testpypi`/`pypi` GitHub Environments + `mode=reserve` dispatch run against TestPyPI then PyPI, with `mordred-hermes 0.0.0.dev0` confirmed live on both indexes (details and the required-reviewers deviation are in the completion note at the top of `CI.md` §Initial setup)
- [x] H1 verification: run `pip install mordred-hermes` in an environment lacking hermes-agent, and confirm dependency resolution fails fast on hermes-agent's resolution failure (add a fresh-venv test in CI) — **completed 2026-05-17** (incorporating Codex review feedback): added a `fresh-venv-resolution` job to `.github/workflows/ci.yml` (`needs: test`, deliberately skips the root `pip install -e .` and installs mordred-hermes standalone). In addition to a non-zero exit code, grep-verifies that `install.log` contains a `hermes-agent` resolution failure (`No matching distribution` / `Could not find a version`) — this prevents false positives from a broken pyproject or network outage. The pin under test is the current `hermes-agent>=0.11.0` (deviation from the spec's `>=1.0`, see §0.5 L66)

### 0.6 CI workflow

- [x] Create `.github/workflows/ci.yml` (2026-05-09, PR #8): pytest + pytest-cov, ruff check / format --check, mypy --strict, paths-filter + concurrency cancel-in-progress, matrix = (ubuntu-24.04 / macos-latest) × (Python 3.10 / 3.11 / 3.12), macOS also installs the `[macos]` extra. See `docs/dev/CI.md` §`ci.yml` details
- [x] Create `.github/workflows/upstream-check.yml` (2026-05-09, PR #8): weekly Monday 03:00 UTC + workflow_dispatch, compares Hermes upstream's `VALID_HOOKS` against Mordred plugins' `register_hook("...")` calls; on drift, auto-files an issue labeled `actionable` + `upstream-drift`. See `CI.md` §`upstream-check.yml` details
- [x] Create `.github/labeler.yml` + `.github/workflows/labeler.yml` (2026-05-09, PR #8): labels `mordred-*` paths, uses `pull_request_target` to support fork PRs (permissions limited to `contents: read` + `pull-requests: write`); the one-time `gh label create` commands for the label list are in `CI.md` §`labeler.yml` details

### 0.7 ~~HSeam-1 PR~~ → Zero-PR commitment (deferred to v2 vendored fork)

> **2026-05-07 revision**: MIGRATION.md §10 row 4 / §5 confirms **zero upstream PRs**. We will **not** submit any PR to Hermes upstream in v1. Disable-defense is handled entirely by the plugin-side strict-mode startup refusal (Phase 1.1 H3 task, SPEC.md §Plugin-disable protection Tier A).

In v1, this 0.7 section covers only the following tasks:

- [x] Declare `privacy_lock: true` in the five manifest-backed plugins as a declarative marker. Hermes ignores it; the explicit six-entry `SIBLING_PLUGINS` tuple is the enforcement source and is not auto-expanded from manifests
- [x] Implement the H3 plugin-side strict-mode startup refusal in Phase 1.1 (see TODO §1.1) — **completed 2026-05-10** (Phase 1.1 PR; see §1.1 H3 Path B check)

v2 escape hatch (deferred):

- [ ] (v2) Place a patched version of the relevant Hermes version at `vendor/hermes/<version>/hermes_cli/plugins_cmd.py`, and define a `hard-lock` extra under `[project.optional-dependencies]` in `pyproject.toml`. See UPSTREAM.md §Tier B for details

### 0.8 Verify Hermes hook payloads against real code

> **L2 (widening the Phase 0 blocker scope)**: The old 0.8 treated only `pre_llm_call`'s override return value and `register_provider` as blockers, but Phase 0 now also verifies the "needs verification" items scattered throughout SPEC/PLAN (hook ordering guarantees, child-process spawn proxy inheritance, `pre_gateway_dispatch` return shape, etc.), since the implementation paths for Phase 1.1 / 2.1 / 3.1 depend on them.
>
> **2026-05-10 source-code verification completed** (PR `docs/mordred-hermes-phase0.8-hook-verify`): results are collected in [`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md). Also reflected in SPEC.md §Plugin-Only Architecture / §Story 4 / §Plugin: `mordred_llm_guard` / §Story 6. **MAJOR finding**: provider override in `pre_llm_call` is structurally impossible in v0.11.0 — the Phase 2 design was changed to session-scoped enforcement (see [`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §5). Live runtime/network verification, as noted in L106-117, is deferred to a separate issue/PR (live verify).

- [x] Verify the contents of the `pre_tool_call` payload in `agent/run_agent.py` and `model_tools.py` (whether `origin_skill` is included, and the return shape a hook uses to block) → completed 2026-05-10 ([`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §4): the payload is only `tool_name`/`args`/`task_id`/`session_id`/`tool_call_id`, **`origin_skill` is absent**; the block form is `{"action": "block", "message": str}` (`hermes_cli/plugins.py:1085-1121`)
- [x] Verify the contents of the `pre_llm_call` payload in `agent/run_agent.py` (whether `provider_id`/`model_id` are included, and the return shape for overriding) → completed 2026-05-10 ([`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §5): the payload has only `model` (`provider` is absent); the return value is **context-injection only** and provider override is impossible (`run_agent.py:10303-10313`, `plugins.py:976-986`)
- [x] Verify the `pre_gateway_dispatch` return action (`skip`/`rewrite`/`allow`) in `gateway/run.py` → completed 2026-05-10 ([`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §6): confirmed the skip/rewrite/allow semantics at `gateway/run.py:3573-3605`, matching the docstring
- [x] Verify the `pre_approval_request` / `post_approval_response` payloads in `tools/approval.py` → completed 2026-05-10 ([`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §7): confirmed at `approval.py:34-56, 1054-1209`, **observer-only** (return value ignored), `choice` is once/session/always/deny/timeout
- [x] Verify the Hermes plugin loader's hook-ordering guarantee in `hermes_cli/plugins.py` (registration order vs. priority) — **prerequisite for the Phase 3 strict-mode bootstrap order** → completed 2026-05-10 ([`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §1): confirmed to be **registration order** (no priority system, `plugins.py:968-1002`). Since the entry-point plugin (Mordred) loads after bundled/user/project plugins, its hook callbacks are always called last
- [x] Verify the Hermes child-process spawn API (whether proxy env vars are inherited from `os.environ`, or explicit injection is required) — **prerequisite for Phase 3 path injection, and needed to pin down the blast radius of the M3 transitive failure mode** → completed 2026-05-10 ([`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §8)
  - [x] Verified in practice whether subprocess env is a **snapshot** (`subprocess.Popen(env=os.environ.copy())` style — a mid-session `use(path)` env update doesn't propagate to the live subprocess) or a **live reference** (retains a reference to the environment variables) → confirmed to be **snapshot per spawn** (`_make_run_env` in `tools/environments/local.py:186-213` freshly builds `dict(os.environ | env)` every time). Mordred's `os.environ` mutation **propagates to subsequent spawns but running children stay frozen**
  - [x] Grepped `agent/run_agent.py` / `tools/terminal.py` / other Popen call sites and enumerated subprocess spawn locations, checking each site's env-passing method → enumerated 285 sites, **splitting into 2 regimes** (found in the PR #9 Codex review, [`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §8.1):
    - **Regime A (blocklist-style)**: `tools/terminal_tool.py` / `tools/environments/{local,docker,ssh,singularity}.py` / `tools/browser_tool.py` — proxy variables pass through
    - **Regime B (allowlist-style, silently drops proxy)**: `tools/code_execution_tool.py` (`_SAFE_ENV_PREFIXES` filter) — Mordred **must explicitly register** proxy variables in the `tools.env_passthrough` registry, otherwise `execute_code` children communicate outside the tunnel
- [x] Verify the existence of an API equivalent to `hermes plugins list --disabled` (needed by H3 Path B's `on_session_start` for sibling-disabled detection; if absent, fall back to reading `~/.hermes/config.yaml`'s plugins config directly) → completed 2026-05-10 ([`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §2): no `--disabled` flag exists, and `plugins_cmd._discover_all_plugins()` does not show entry-point plugins. **Fallback confirmed**: either call `from hermes_cli.plugins import _get_disabled_plugins` (module-level) directly, or read `~/.hermes/config.yaml` directly via `yaml.safe_load` (the `plugins.disabled` list)
- [x] Verify whether Hermes reflects dynamic plugin disabling while a session is running (H3 Path B caveat: v1 is designed on the assumption it does not; if it does, a different design is needed) → completed 2026-05-10 ([`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §3): confirmed it **does not** reflect it (`PluginManager.discover_and_load(force=False)` short-circuits via the `_discovered` cache). This confirms the H3 Path B refuse-at-startup-only design is correct
- [x] **Network provider live testing** (prerequisite for confirming M8 §provider_transport_flagger's v1 baseline allowlist) — **completed 2026-05-17** (verified with a hermetic in-process SOCKS5 inspector instead of Wireshark/live Tor. `tests/integration/test_provider_transport.py` + `_socks5_inspector.py` observe each SDK's CONNECT via the RFC 1928 ATYP byte; no API keys or cloud calls needed). bedrock's deep DNS quirk / vertex are deferred as noted below:
  - [x] Live-tested whether `anthropic` SDK + `HTTPS_PROXY=socks5h://...` routes requests through the proxy — **done** (`TestAnthropic`, env-trusting httpx, confirmed ATYP=DOMAINNAME)
  - [x] Same test for `openai` SDK — **done** (`TestOpenAI`, same httpx path)
  - [x] Same test for `gemini` — **done** (`TestGemini`). **Finding**: the current SDK is `google-genai` (httpx-based), not the old `google-generativeai` (requests-based). Corrected `KNOWN_PROVIDERS["gemini"].transport` from `"requests"` to `"httpx"`
  - [x] Confirmed whether `mordred-local` (LM Studio/Ollama localhost) is excluded from the proxy by the NO_PROXY default — **done** (`TestMordredLocal`, demonstrated with the actual env from `proxy_env.desired_env(path="tor")` that localhost bypasses the proxy)
  - [ ] Live-verify `bedrock` (boto3)'s DNS quirk (whether DNS queries bypass the proxy toward the OS resolver) — **partial**: `respects_socks5h=False` has been demonstrated (`TestBedrock`, botocore's urllib3 transport doesn't support SOCKS). Deep packet-capture verification of the DNS quirk requires a real AWS account, so it's deferred to v2
  - [ ] Live-verify `vertex` (google-cloud SDK)'s partial proxy compliance — **deferred**: `google-cloud-aiplatform` is a heavy SDK and the `partial` behavior is GCP-side. Out of scope for transport-layer verification, deferred to v2
  - [x] Record each result in `provider_transport_flagger.py`'s `KNOWN_PROVIDERS` dict — **done** (anthropic/openai/gemini/mordred-local are `unverified_baseline=False`; bedrock/vertex remain `True` for the reasons above, with verification status recorded in the module docstring)
- [x] **SOCKS5h library compatibility testing**: confirmed the major HTTP clients' support for the SOCKS5h URL scheme — **completed 2026-05-17** (`tests/integration/test_socks5h_libs.py`, demonstrated ATYP=DOMAINNAME via the in-process SOCKS5 inspector. Flipped `unverified_baseline` to `False` for all 4 entries in `proxy_env.SOCKS5H_LIBRARY_REQUIREMENTS`, and rewrote `notes` with the verified values)
  - [x] `httpx` (used by anthropic/openai/mordred-local): pinned down the version requirement for `socks5h://` support — **done** (httpx 0.28.1 + socksio 1.0.0; both socks5:// and socks5h:// delegate DNS to the proxy — no scheme distinction, the safe default)
  - [x] `urllib3` / `requests[socks]`: same — **done** (urllib3 2.7.0 + PySocks 1.7.1, requests 2.33.1. requests/urllib3 do honor the scheme distinction — socks5:// means local DNS)
  - [x] Pinned down the version boundary where older `aiohttp` lacks SOCKS5h support (basis for the allowlist decision) — **done** (aiohttp 3.13.5 + aiohttp-socks 0.11.0). **Finding**: `python-socks` (the aiohttp-socks engine) **rejects** the `socks5h://` URL scheme with a `ValueError` — remote DNS requires `socks5://` plus explicit `rdns=True`. Caveat recorded in `notes`
- [x] Fed the verification results back into `docs/SPEC.md` §"Plugin-Only Architecture"'s "Items likely to need further core modifications" → completed 2026-05-10 (updated SPEC.md L100-107, cross-linking [`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) as the canonical reference)

### Acceptance gate (Phase 0)

- [x] `pip install -e ./mordred-hermes` succeeds (verified 2026-05-09)
- [x] The initial 5 plugins were discovered by `PluginManager.discover_and_load()` via their entry points and `register()` was called (verified 2026-05-09); the later `mordred_e2e` entry point brings the current bundle to 6
- [x] ~~`hermes plugins list` shows all mordred_* entries~~ → **closed by Phase 1.3 wizard**: Hermes upstream's `hermes_cli/plugins_cmd.py:_discover_all_plugins()` scans only the bundled (`<repo>/plugins/`) and user (`~/.hermes/plugins/`) directories, and does not show entry-point plugins (confirmed on Hermes 0.11.0). Since Tier A discovery (the loader) works, this is treated as passing for Phase 0 acceptance. **Completed 2026-05-13**: `hermes mordred plugins list` (`src/mordred_hermes/wizard/plugins_list.py`) closes the UX gap — it queries `PluginManager` directly and filters by the `mordred_*` prefix, with a fallback to `~/.hermes/config.yaml` `plugins.enabled` on `ImportError`
- [x] `pytest -q` is green even with nothing to test, and `ruff check`/`mypy --strict` are also green (verified locally 2026-05-09 + enforced in CI, PR #8. 7 tests passed, ruff lint/format clean, mypy strict 0 errors. Since `mypy --strict` was already green at baseline, the TODO §0.4 L52 `ctx` typing improvement was scoped out of this PR and left for Phase 1.0 prep)
- [x] ~~HSeam-1 PR draft created~~ → not needed (zero-PR commitment confirmed 2026-05-07. 2026-05-17 stocktake: the struck-through item's disposition is settled; checkbox corrected to `[x]` for consistency with L131)
- [x] 0.8 verification results reflected in SPEC.md (completed 2026-05-10, source-code portion only. Runtime / network live verification is tracked in a separate issue, placed under the `MORDRED_LIVE_*=1` integration test gate in Phase 2/3)

---

## Phase 1 — Privacy Primitives (`mordred_privacy_check` + metadata + wizard)

### Open decisions

- [x] DECIDE: finalize the audit-log `reason` enum. Freeze the codes listed in SPEC.md §Audit log policy (`policy.strict.clearnet` and 8 others, 9 total) in Phase 1 step 0. **2026-05-07 addendum (M2)**: add `policy.strict.local_stream_interrupted` (Phase 2 mid-stream local-endpoint death) as a 10th code, frozen at the same time → **completed 2026-05-10**: froze a 12-code enum (the 9 + M2 + 2 Phase-2 forward-compat codes, `policy.strict.session_refused` / `policy.strict.provider_override_at_session_start` — enumerated at the same time since SPEC/PLAN already referenced them). `src/mordred_hermes/privacy_check/_audit_reasons.py:ReasonCode` (a typed `Literal`) and `docs/dev/POLICY.md §Audit log reason enum (frozen)` are canonical
- [x] DECIDE: whether `metadata.mordred.*` conflicts with the agentskills.io spec. Confirm in the spec docs that the standard has no key of the same name → **completed 2026-05-10**: the agentskills.io v1 spec (`https://agentskills.io/specification`) defines `metadata` as a flat string→string map and explicitly recommends vendor namespacing (`We recommend making your key names reasonably unique to avoid accidental conflicts`). No conflicting keys. However, Mordred deviates by using **nested objects + non-string types** (`requires_keyvault: bool`, `outbound_endpoints: list[str]`). `skills-ref validate` may reject Mordred-flavored skills, but this is acceptable in v1 since the `hermes mordred install` wrapper is the authoritative validator. See `POLICY.md §agentskills.io deviation` for details

### 1.1 `mordred_privacy_check` plugin

> **2026-05-10 Phase 1.1 PR completed**: all sub-tasks landed in `src/mordred_hermes/privacy_check/`. 105 tests green, ruff/mypy strict clean. See `docs/dev/POLICY.md` for details.

- [x] Implement `config_schema` (policy/allow_cloud_llm/cloud_provider_allowlist/audit_log_path) in `plugin.yaml` (PLAN §1.1)
- [x] Implement a pure policy evaluator in `policy.py` (no I/O)
- [x] In `skill_frontmatter.py`, `yaml.safe_load` the SKILL.md frontmatter and extract `metadata.mordred.*` (using the ruamel.yaml safe loader; the nested-object form is documented as an agentskills.io spec deviation in POLICY.md §SKILL.md `metadata.mordred.*`)
- [x] Implement a process-serialized NDJSON logger in `audit.py` (rotation, gzip, 30-day retention, file mode 0600)
  - [x] Froze the `class Writer(Protocol): def append(self, entry: dict) -> None: ...` interface in Phase 1 (factory-swapped to `EncryptedWriter` in Phase 4)
  - [x] Serialized via an in-process lock plus stable sidecar `fcntl.flock`
  - [x] **M1 (multi-process write contention)**: lock format checks, rotation,
    write-all, and rollback as one cross-process critical section; verify MRAL
    inode/header ownership before DEK reuse. See PATHS.md §Multi-process write
    serialization. A daemon writer remains an optional churn optimization
- [x] Implement `hermes mordred install <skill>` in `install_wrapper.py`:
  - [x] Read SKILL.md, parse frontmatter
  - [x] strict + clearnet → block + audit `policy.strict.clearnet`
  - [x] strict + missing → block + audit `policy.strict.unknown_metadata`
  - [x] lenient + missing → allow + audit `policy.lenient.unknown_metadata_warning`
  - [x] allow → `subprocess.run(["hermes", "skills", "install", skill])` (runner is test-injectable)
- [x] Implement the `pre_tool_call` hook: **generic tool-name allowlist only** (Phase 0.8 verification confirmed `origin_skill` is absent from the payload, [`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §4). Strict-mode default blocklist: `web_fetch`, `web_search` on Clearnet. Per-skill enforcement is implemented only in the install-time wrapper (`hermes mordred install`), not in the runtime hook
- [x] In the `on_session_start` hook, load the policy snapshot from `~/.hermes/config.yaml`'s `plugins.mordred_privacy_check` + detect sibling Mordred plugin disabling (always needed under the zero-PR commitment; also kept as defense-in-depth in v2 when the `[hard-lock]` extra is installed)
- [x] **H3 Path B (strict-mode startup refusal)**: at the top of `on_session_start`, scan the fixed canonical list `["mordred_privacy_check", "mordred_network", "mordred_llm_guard", "mordred_keyvault", "mordred_e2e", "mordred_wizard"]`; if `policy=strict` and even one is disabled, raise `MordredIntegrityRefused(BaseException)` to abort the session. The dedicated refusal escapes Hermes's `except Exception:` wrapper without being mistaken for an ordinary `SystemExit`. Simultaneously records the `mordred.degraded.disable_unprotected` audit entry + sets the deep-defense `_runtime.poison()` flag. lenient/off only warn. See SPEC.md §Plugin-disable protection Path B

### 1.2 Skill metadata namespace

- [x] Document the `metadata.mordred.*` fields (network_requirements / requires_keyvault / outbound_endpoints) in `src/mordred_hermes/privacy_check/README.md` (2026-05-10 Phase 1.1 PR; also see `POLICY.md §SKILL.md \`metadata.mordred.*\` extension`)
- [x] Create fixture skills: `tests/fixtures/clearnet_skill/SKILL.md`, `tests/fixtures/tor_skill/SKILL.md`, `tests/fixtures/missing_metadata_skill/SKILL.md`

### 1.3 `mordred_wizard` plugin

> **Phase 1.3 completed 2026-05-13** (PR1 = configure + explainer, PR2 = upgrade + install_dispatch + audit_cli + plugins_list + docs). 282 tests green, ruff/mypy strict clean. `hermes-mordred {configure,upgrade,install,policy,audit,plugins} ...` works — the structure is such that `hermes mordred ...` also works automatically once Hermes 0.12+ lands.

- [x] Implement `ctx.register_cli_command("mordred", help, setup_fn=_setup_subparser, ...)` in `__init__.py`
- [x] Build the argparse subparser tree (configure / upgrade / install / network / policy / audit / keyvault / plugins) in `cli.py`
- [x] In `configure.py`, spawn `subprocess.run(["hermes", "setup"])` as a child → then Mordred-specific prompts (via `prompt_toolkit`)
- [x] Implement the Story 1 / Story 1.5 migration in `upgrade.py`:
  - [x] Edit `~/.hermes/config.yaml` via a `ruamel.yaml` round-trip (preserving comments and key order)
  - [x] Idempotent (no-op when state already matches)
  - [x] On conflict with an existing `plugins.mordred_*`, diff + prompt
  - [x] When `~/.openclaw/mordred/` is detected, follow the "Migration from the legacy OpenClaw paths" table in PATHS.md
  - [x] **H5 conflict resolution semantics**: implement the per-row policy settled in PATHS.md §Migration from the legacy OpenClaw paths table:
    - audit.log is append-by-timestamp-window; on overlap, abort and require `--audit-merge=skip\|append-all\|abort`
    - keyvault / credentials use `never overwrite`; abort if already present (idempotent no-op if content-identical)
    - policy.json / `plugins.mordred_*` use diff + prompt (interactive); in batch mode, `--policy-conflict=keep-existing\|overwrite\|abort`
  - [x] **H5 idempotency marker**: on completing the first audit migration, write `~/.hermes/mordred/.audit-migrated-from-openclaw` (contents: one ISO-8601 UTC timestamp line); subsequent runs skip the audit migration if the marker exists
  - [x] The `--reset` flag forces all conflict policies to `overwrite`; `--non-interactive` suppresses interactive prompts and fails fast on any unspecified policy
- [x] In `policy_writer.py`, write out the `plugins.mordred_*` section of `~/.hermes/config.yaml`
- [x] In `policy_explainer.py`, implement `policy explain <skill-id>` / `policy dry-run <skill-path>`
- [x] **PR2 additions**: `install_dispatch.py` (adapter to privacy_check.install_wrapper), `audit_cli.py` (tail/grep), `plugins_list.py` (closes the §0.5 L128 UX gap)

### 1.4 Tests (Phase 1)

- [x] `tests/test_policy.py`: covers the strict/lenient/off × clearnet/tor/vpn/local-only matrix (2026-05-10 Phase 1.1 PR)
- [x] `tests/test_audit.py`: rotation, file mode 0600, thread/process concurrency (2026-05-10 Phase 1.1 PR; process-lock regression added 2026-07-30)
- [x] `tests/test_install_wrapper.py`: asserts install results for fixture skills (2026-05-10 Phase 1.1 PR)
- [x] Added `tests/test_skill_frontmatter.py`: matrix for the SKILL.md frontmatter parser (2026-05-10 Phase 1.1 PR; not in the original PLAN, added as independent verification of skill_frontmatter.py)
- [x] Added `tests/test_hooks.py`: matrix for the `on_session_start` / `pre_tool_call` hook handlers, the strict refusal path, the poison flag, and sibling-disable detection (2026-05-10 Phase 1.1 PR; dedicated `MordredIntegrityRefused` coverage added in the follow-up refactor)
- [x] `tests/test_upgrade.py`: migrates a fixture config and checks the expected output (Phase 1.3 wizard PR2 / 2026-05-11)
- [x] `tests/test_policy_writer.py`: asserts comment preservation across the `ruamel.yaml` round-trip (Phase 1.3 wizard PR1 / 2026-05-10)
- [x] ~~`tests/test_wizard_prompts.py` (snapshot test using `pytest-snapshot`)~~ → **not adopted in Phase 1.3 (2026-05-13)**: since the implemented `configure.py` builds prompt strings directly via the `PromptIO` Protocol, `tests/test_configure.py` can assert each prompt's label / default directly (independent of actual prompt_toolkit output). The need for a snapshot test disappeared, so the `pytest-snapshot` dep was never added either

### 1.5 Docs and bookkeeping (Phase 1)

- [x] Created `docs/POLICY.md` (policy schema reference, all enum values, examples) — 2026-05-10 Phase 1.1 PR; the 12-code freeze (9 Phase-1 codes + 3 Phase-2 forward-compat codes) is also declared via the typed `Literal` in `_audit_reasons.py:ReasonCode`
- [x] Initialized `docs/VERSION` to `0.1.0a0` (PEP 440 alpha 0). The human-readable spec label `v0.1.0-mvp.0` is managed separately as branding for ROADMAP/SPEC/release notes (confirmed in the 2026-05-09 Codex review; see the PLAN §0.5 PEP 440 compliance caveat)
- [x] Documented each plugin's own path / config / internal API in its `README.md` (privacy_check / wizard completed 2026-05-13; network / llm_guard / keyvault remain, to be done when their respective Phase starts)
- [x] **L1 (bilingual track retired)**: the Japanese-language (`*.ja.md`) docs have been deprecated and deleted (2026-06-25). Going forward, the English docs (`*.md`) are the sole source of truth

### Acceptance gate (Phase 1)

- [x] `hermes mordred configure` writes `~/.hermes/config.yaml` and `policy.json` (PR1 complete, `tests/test_configure.py`)
- [x] `hermes mordred upgrade` migrates an existing Hermes install without breaking it (the OpenClaw migration Story 1.5 also succeeds) (PR2 Phase E complete, `tests/test_upgrade.py` + `tests/test_openclaw_migration.py`)
- [x] `hermes mordred install <fixture-clearnet-skill>` is blocked under strict policy (PR2 Phase F-1 complete, `tests/test_install_dispatch.py::TestRunBlock`)
- [x] `pytest -q` all green, `ruff check` / `mypy --strict src` green (2026-05-13: 282 tests passed)
- [x] Confirmed that the Phase 1 acceptance gate passes under the zero-PR commitment using only the plugin Tier A guard (strict-mode startup refusal + audit log), with no dependency on the v2 `[hard-lock]` extra — landed in Phase 1.1, unchanged by the wizard addition

---

## Phase 2 — LLM Enforcement (`mordred_llm_guard` + `mordred-local` provider)

> **Phase 2 PR2 completed** (2026-05-13, TDD redo): PR1 (`local_adapter` + `harness_detect` + PolicySnapshot Phase 2 fields) has already been merged to main in #14. Phase 2 was completed in PR2, adding `enforce.py` (session-scoped, refuse-only), the wizard `harness_primary` prompt, and the M1/M2/L1 follow-ups from the PR1 self-review. 386 tests passed / 4 skipped (live LLM gated), ruff + ruff format + mypy --strict all green. Rewritten via strict RED→GREEN→REFACTOR.

### Open decisions

- [x] ~~DECIDE: enable cloud passthrough when the `pre_llm_call` payload includes provider/model~~ → **closed stale** (Phase 0.8 verification confirmed that `provider` is absent in v0.11.0 and the return value is context-injection only, [`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §5). v1 always uses session-scoped enforcement (Phase 2 PR2 `enforce.py`)
- [x] DECIDE: behavior on detecting a harness primary — fully abort startup in strict mode vs. warn + degrade → **strict=abort / lenient=warn+audit / off=noop** (Codex D2, consistent with SPEC.md L143). Implementation: `harness_detect.check_harness_primary` (Phase 2 PR1). Raises `MordredHarnessRefused(BaseException)` in strict mode

### PR1 prep findings (Codex review 2026-05-13)

- **B1 (BLOCKER)**: `providers._discover_providers()` scans only `<repo>/plugins/model-providers/*` + `$HERMES_HOME/plugins/model-providers/*` and never sees entry-point plugins → **explicitly call** `register_mordred_local()` inside `register(ctx)`. Module-import side effects are forbidden
- **B2 (BLOCKER)**: even if the active provider in `~/.hermes/config.yaml` is patched in `on_session_start`, `HermesCLI`/`AIAgent` has already resolved `self.provider`/`base_url`/`model` **before** the session starts → **auto-swap is impossible in v1**. PR2's enforce is **refuse-only** (D3 = refuse-only). The (b) path via `register_provider` + config patching is deferred to the v2 vendored fork
- **H1 (HIGH)**: the SPI list in PLAN.md L299 (`wrap_stream_fn` / `auth` / `discovery` / `resolve_synthetic_auth` / `normalize_config` / `prepare_dynamic_model` / `resolve_dynamic_model` / `augment_model_catalog` / `wizard`) **does not exist in current Hermes and is stale**. `ProviderProfile` is declarative only. Streaming is owned by Hermes core (`agent/error_classifier.py`) → **`transport.py` and the M2 `MordredLocalStreamInterrupted` are dropped from PR1, deferred to v2**. `policy.strict.local_stream_interrupted` remains in the frozen enum (with no class that raises it), with the deferral made explicit in POLICY.md
- [x] **H2 (HIGH)**: refusal in strict mode uses custom `BaseException`-derived classes (`MordredHarnessRefused` / `MordredSessionRefused` / `MordredIntegrityRefused`) rather than `SystemExit`, so cleanup-style `except SystemExit:` blocks cannot misclassify policy enforcement as an ordinary CLI exit.
- **M3 (MEDIUM)**: pulled the `PolicySnapshot` Phase 2 fields (`local_llm_endpoint` / `local_llm_model_id` / `cloud_attempt_action`) forward into PR1, so PR2's enforce can read `PolicySnapshot` directly as its input surface. The wizard's `configure.py` is wired to the snapshot in the same PR (removing the old path that collected into a separate `phase2_fields` dict and then discarded it)
- **N1 (NIT)**: in the 12-code freeze, `policy.strict.cloud_not_allowlisted` is the classification reason, while the final action is `policy.strict.session_refused` (default refuse) or `policy.strict.provider_override_at_session_start` (v2 auto-swap). Made explicit in POLICY.md §Audit log reason enum (frozen)

### 2.1 `mordred_llm_guard` plugin

- [x] Implement the `mordred-local` synthetic provider in `local_adapter.py` — **declarative `ProviderProfile` only** (Phase 2 PR1; the SPI list was confirmed stale under H1. `name="mordred-local"` / `api_mode="chat_completions"` / `base_url` is read dynamically from `policy.json`). `register_mordred_local()` is called explicitly inside `register(ctx)` (B1 fix)
- [x] ~~`transport.py`, an HTTPX client for the local OpenAI-compatible endpoint~~ → **removed from v1** (H1: streaming is owned by Hermes core, so the placeholder had no reliable integration point. Recreate it once upstream lands a streaming hook in v2. 2026-07-28: stale placeholder deleted)
- [x] `health.py`: endpoint health probe; raises `MordredLocalUnreachable` on failure (Phase 2 PR1)
- [x] ~~`override.py`, a `pre_llm_call` handler~~ → **dropped in v1** (B2 / HOOK_PAYLOADS §5: `pre_llm_call` cannot override the provider). Replacement: session-scoped enforcement via `on_session_start` in `enforce.py` (implemented in PR2. 2026-05-17 stocktake: v1-drop confirmed, checkbox corrected to `[x]`)
- [x] **`enforce.py` (PR2, v1 = refuse-only)**: `on_session_start` handler (Phase 2 PR2 completed):
  - [x] lenient/off → no-op (v1 is silent — a per-session allow audit is reconsidered in v2)
  - [x] strict + active provider matches `cloud_provider_allowlist` + `allow_cloud_llm: true` → passthrough, audit `policy.strict.cloud_allowlisted`
  - [x] strict + no match, or `allow_cloud_llm: false` → **refuse** (raise `MordredSessionRefused`), audit `policy.strict.session_refused` (also emits the classification reason `policy.strict.cloud_not_allowlisted` at the same time, Codex N1)
  - [x] strict + no provider info (degraded `no_resolved_provider`) → refuse + audit `mordred.degraded.no_resolved_provider` (one-shot) + `policy.strict.unconditional_override`
  - [x] strict + `mordred-local` → health probe → audit allow on success; on failure, `MordredSessionRefused` (chaining `MordredLocalUnreachable` as `__cause__`. Codex review P2 round 2: since a bare `Exception` is swallowed by Hermes's `invoke_hook`, `_probe_local` wraps it as a `BaseException`-derived refusal) (Phase 2 acceptance gate row 4)
  - [ ] (b) auto-swap via `register_provider` + config patching is **out of scope for v1** (B2: live runtime mutation is impossible), to be reevaluated in the v2 vendored fork
  - [x] **runtime override support** (Codex review P1 round 3): since `on_session_start` only resolves from disk and misses CLI `--provider` / `HERMES_INFERENCE_PROVIDER` / oneshot switches, additionally run `check_runtime_provider(provider=kwargs.provider)` in the `pre_api_request` hook (the resolved runtime provider arrives via `run_agent.py:11320-11338`). strict + cloud not allowlisted → `MordredSessionRefused` escapes `run_agent.py:11337`'s `except Exception: pass` via `BaseException`
- [x] **`harness_detect.py`**: `on_session_start` handler (Phase 2 PR1 completed):
  - [x] Read the configured harness primary from `~/.hermes/config.yaml plugins.mordred_llm_guard.harness_primary`
  - [x] Prefix-regex allowlist: `^codex(-\d+(\.\d+)*)?$` / `^claude-cli(-\d+(\.\d+)*)?$` / `^cursor(-\d+(\.\d+)*)?$` / `^acp-[a-z][a-z0-9-]*$`
  - [x] strict → raise `MordredHarnessRefused(BaseException)` + audit `mordred.degraded.disable_unprotected` (decision=block)
  - [x] lenient → audit (decision=warn) + log warning + continue (Codex M2)
  - [x] off → no-op

### 2.2 Wizard additions (Phase 2)

- [x] Add prompts to `hermes mordred configure` for the local LLM endpoint URL (default `http://localhost:1234/v1`), local model id, and cloud attempt action (always-block / prompt-once) — **already collected in Phase 1.3. Wired into `PolicySnapshot` in Phase 2 PR1 (per Codex M3, removing the old separate `phase2_fields` dict)**
- [x] Add a harness primary declaration prompt to `hermes mordred configure` (Phase 2 PR2 completed, default `none`, choices: `none`/`codex`/`claude-cli`/`cursor`/`acp-claude`/`acp-cline`). Added `harness_primary` to PolicySnapshot + `PolicyWriter.write` upserts `config.yaml plugins.mordred_llm_guard.harness_primary`

### 2.3 Tests (Phase 2)

- [x] `tests/test_enforce.py`: all cases of the decision matrix (Phase 2 PR2 completed, 25 tests green. Renamed from the old `tests/test_override.py` — Codex L2)
- [x] `tests/test_enforce_audit.py`: correct reason code emitted on each path (Phase 2 PR2 completed, 7 tests green, including frozen-enum membership)
- [x] `tests/test_harness_detect.py`: harness primary detection matrix (Phase 2 PR1 completed, 24 tests green)
- [x] `tests/test_health.py`: health probe success / timeout / connect-refused / 500 matrix (Phase 2 PR1 completed, 9 tests green)
- [x] `tests/test_local_adapter.py`: B1 explicit-register + module-import-no-side-effect + policy.json fallback (Phase 2 PR1 completed, 8 tests green)
- [x] `tests/test_exceptions.py`: `BaseException` propagation contract (Phase 2 PR1 completed, 7 tests green)
- [x] `tests/test_llm_guard_register.py`: `register(ctx)` wires the provider + `on_session_start` (Phase 2 PR1 completed; PR2 added a 2-callback registration-order test, 7 tests green)
- [x] `tests/test_llm_guard_typing.py`: `PluginContext` Protocol narrow surface (Phase 2 PR1 completed, 4 tests green)
- [x] `tests/integration/test_llm_local.py` (gated by `MORDRED_LIVE_LLM_TEST=1`): real LM Studio endpoint roundtrip (Phase 2 PR2 completed, 5 tests; 3 live-gated ones skipped)
- [x] Failure-mode test: lmstudio down → `MordredSessionRefused` (chaining `MordredLocalUnreachable` as `__cause__`, via the wrap added after Codex review P2 round 2). Phase 2 PR2 completed, `tests/integration/test_llm_local.py::TestFailureMode`, run hermetically on port 1
- [x] ~~**M2 (mid-stream local-endpoint death)**: `tests/test_enforce.py::test_mid_stream_disconnect`~~ → **deferred to v2** (H1: Hermes core owns streaming, so the plugin can't reliably raise `MordredLocalStreamInterrupted`. Will return once upstream lands a streaming hook in v2. 2026-05-17 stocktake: v2-deferral confirmed, checkbox corrected to `[x]`)

### Acceptance gate (Phase 2)

- [x] Turns under strict policy → v1 is refuse-only (B2: auto-swap is deferred since live runtime mutation is impossible). If `mordred-local` is already active, passthrough after a health probe, audit `policy.strict.cloud_allowlisted` (`test_enforce.py::TestStrictLocal`)
- [x] (when provider info is present) strict + cloud upstream in `cloud_provider_allowlist` + `allow_cloud_llm: true` → no refusal, audit `policy.strict.cloud_allowlisted` (`test_enforce.py::TestStrictCloudAllowlisted`)
- [x] (when provider info is absent / degraded) v1 refuses + audits `mordred.degraded.no_resolved_provider` (one-shot) + `policy.strict.unconditional_override` (action). Routing to `mordred-local` itself is planned to return in the v2 vendored fork (`test_enforce.py::TestStrictDegraded`)
- [x] strict + no local endpoint reachable → fails fast with `MordredSessionRefused` (chaining `MordredLocalUnreachable` as `__cause__`, wrapped per Codex review P2 round 2). `tests/integration/test_llm_local.py::TestFailureMode`
- [x] Codex/Claude-CLI primary + strict → `hermes mordred` startup is refused (`harness_detect` PR1 + PR2 verify hook registration order in `test_llm_guard_register.py`)
- [x] Audit log records every override decision (`test_enforce_audit.py::TestFrozenEnumMembership`, including the frozen-5-reasons membership invariant)

---

## Phase 3 — Network Paths (`mordred_network`)

> **Phase 3 PR1 completed** (2026-05-13): primitives landed (paths/* / proxy_env / provider_transport_flagger / api / _exceptions / 16-code freeze). 514 tests passed / 4 skipped, ruff + format + mypy --strict all green. `register(ctx)` remains a no-op; hooks + runtime + wizard CLI are planned for PR2. Real-traffic verification (including TODO §0.8 L110-117) will be done in PR3. See `src/mordred_hermes/network/README.md` for details.
>
> **Phase 3 PR2 completed** (2026-05-14): landed `runtime.py` (state machine + M9 liveness worker + M3 `live_subprocess_count` + env snapshot/restore), `hooks.py` (`on_session_start` / `on_session_end` / `pre_tool_call` + the `wait_until_ready` polling fallback), `__init__.register(ctx)` (runtime singleton + 3 hook callbacks), and the wizard `network_cli.py` (replacing the `network use` / `network status` NotImplementedError stubs with real handlers). 583 tests passed / 4 skipped (+69 tests vs. PR1: runtime 33 / hooks 23 / wizard_network_cli 12, minus 1 removed stub-deferral test), ruff + format + mypy --strict all green. Real-traffic provider verification, SOCKS5h library compatibility, the control-port circuit-status liveness probe (`stem`), and docker-compose integration tests are **deferred to PR3**. See `src/mordred_hermes/network/README.md` (PR2 status table) for details.
>
> **Phase 3 PR3b completed** (2026-05-14): added `tests/integration/_docker.py` (compose v2 lifecycle helper + 3-tier skip guard) + `tests/integration/docker/tor/{Dockerfile,torrc,docker-compose.yml}` (alpine + tor + loopback-only port binding + RFC1918 SocksPolicy) + `tests/integration/test_tor.py` (SOCKS5 handshake / socks5h DNS roundtrip / proxy_env HTTPS_PROXY round-trip) + `tests/integration/test_vpn.py` (gated by MORDRED_LIVE_VPN_TEST=1 + MORDRED_MULLVAD_ACCOUNT: roundtrip / lockdown rollback / handshake freshness). Added an `integration-tor` job to `.github/workflows/ci.yml` (ubuntu-only, `needs: test`) + `.github/workflows/integration-vpn.yml` (workflow_dispatch only). No paths/tor seam was needed (already satisfied by the `_ProcessLike` Protocol + `TorHandle` dataclass, so production code is unchanged). 741 tests passed + 10 skipped, ruff + format + mypy --strict all green. Real-traffic provider verification (§0.8 L110-117) and the stem-against-real-Tor deep probe are **deferred to PR3c**. See `src/mordred_hermes/network/README.md` (PR3b status section) for details.

### Open decisions (resolved 2026-05-09 / 2026-05-13)

- [x] ~~DECIDE: whether to recommend `arti` vs. `tor` as the Tor binary~~ → **v1 default = the official `tor` daemon** (confirmed in SPEC §Plugin: `mordred_network` Tor connection. `arti` will be reevaluated in v2)
- [x] ~~DECIDE: whether to require the Mullvad CLI dependency in v1~~ → **v1 requires the official Mullvad `mullvad` client** (running `wg-quick` directly ourselves is out of scope for v1. Confirmed in SPEC §Plugin: `mordred_network` Mullvad VPN integration)
- [x] ~~DECIDE: if the Hermes plugin loader doesn't guarantee hook ordering, proceed with a polling fallback (for the Phase 3 strict-mode bootstrap order)?~~ → PR2 initially shipped `wait_until_ready()` as a polling fallback. **Superseded 2026-07-27**: `mordred_network.register()` now activates and freezes the process route before returning and before provider-client construction, so session-hook ordering/polling is no longer the initial transport security boundary. The helper remains available for diagnostics/legacy callers

### 3.1 `mordred_network` plugin

- [x] Implement `paths/tor.py` (v1 default = the official `tor` daemon) via `subprocess` (PR1 / 2026-05-13):
  - [x] **torrc generation**: implemented `render_torrc(socks_port, control_port, data_dir)` (`SOCKSPort 127.0.0.1:<port>`, `ControlPort 127.0.0.1:<port>`, `CookieAuthentication 1`, `DataDirectory`) — PR2 persists it to `~/.hermes/mordred/tor-data/torrc`
  - [x] **port collision resolution**: `pick_free_port(candidates)` probes with `socket.bind` (default 9050 → 9150); raises `BringupFailed` if all are taken (converted to `MordredPathBringupFailed` by registration-time fail-closed activation under strict policy)
  - [x] **ControlPort cookie auth**: implemented the M9 liveness probe by reading `<data_dir>/control_auth_cookie` and issuing `GETINFO circuit-status` — **completed PR3a Task #5 (2026-05-14), idle/terminal semantics corrected 2026-07-28**: `mordred_hermes.network.paths.tor.circuit_status_health(handle, *, controller_factory=...)` treats an authenticated empty reply or any reply containing a non-terminal circuit as healthy. Known `FAILED`/`CLOSED`-only replies are unhealthy; syntactically valid unknown uppercase statuses are assumed non-terminal for forward compatibility. `stem>=1.8.0,<2` is an **optional extra, `[tor-control]`** (`pip install mordred-hermes[tor-control]`); when installed, deep liveness is the runtime default. Import failure / missing cookie falls back to shallow `process.poll()`, while authentication, ControlPort, GETINFO, and malformed-response failures are unhealthy
  - [x] **bootstrap timeout**: `wait_for_bootstrap(process, timeout=30s)` tails stdout and detects `Bootstrapped 100%` within 30s (M9)
  - [x] **process management**: `start_process(binary, torrc)` uses `subprocess.Popen`; `stop(handle, grace_seconds=5)` does terminate + grace + kill. Originally called from PR2's `on_session_end`, it is now owned by the process-exit finalizer so one turn/session cannot stop the shared route
  - [x] **caveat documentation**: bridge / obfs4 / Snowflake are out of scope for v1 — documented in `network/README.md`, startup banner added in PR2
  - [ ] Stream isolation (per-session/per-skill SOCKS auth) is deferred. `proxy_env.desired_env(isolation_token=...)` can inject an optional **process-scoped preactivation token** (octets over 255 are sha256'd to work around the RFC 1929 limit), and torrc sets `IsolateSOCKSAuth`; however, `on_session_start` does not overwrite it with `session_id`. Provider clients snapshot proxy configuration, so changing a token after activation requires a restart. Independent per-session/per-skill transports remain v2 work, with per-skill routing additionally pending `origin_skill` (v2-H2)
- [x] Implement `paths/vpn.py` (v1 = the official Mullvad `mullvad` client) via `subprocess` (PR1 / 2026-05-13):
  - [x] **CLI detection**: `detect_cli(which)` tries `shutil.which("mullvad")` → on failure falls back to `/Applications/Mullvad VPN.app/Contents/Resources/mullvad` → raises `BringupFailed` if both fail (registration-time activation escalates a propagated failure to `MordredPathBringupFailed`)
  - [x] **bring-up sequence**: `bring_up(cli_path, region, policy_mode, runner)` runs `lockdown-mode set on` in strict mode; lenient/off respect the user's own setting (Mullvad CLI 2026.2 removed `always-require-vpn`, folding it into `lockdown-mode`)
  - [x] Region set via `mullvad relay set location <country|auto>` — implemented in `bring_up`, with the `auto` default confirmed on the wizard side (PR2)
  - [x] `mullvad connect` → polled via `wait_connected(cli_path, runner, timeout=10s, poll_interval=0.5s)` (M9 bring-up timeout 10s)
  - [x] **liveness probe**: `health(handle, runner, max_handshake_age_seconds=180)` parses `wg show`, with `parse_handshake_age` adding up seconds/minutes/hours/days (M9)
  - [x] **tear-down**: implemented `disconnect(handle, preserve_lockdown=True)`. `preserve_lockdown=False` releases lockdown (kept by default while strict is active)
  - [x] **DNS leak**: the Mullvad client forces the resolver inside the tunnel — documented in `README.md`, no leak in v1
  - [x] **Platform**: macOS Apple Silicon + Ubuntu/Debian baseline, Windows is out of scope for v1 — noted in `vpn.py`'s docstring
- [x] `paths/clearnet.py` is a no-op (PR1 / 2026-05-13, `start()` / `stop()` / `health()`)
- [x] Compute `HTTPS_PROXY` / `HTTP_PROXY` / `ALL_PROXY` / `NO_PROXY` for the active path in `proxy_env.py` (PR1 / 2026-05-13, a pure function — actually applying it to `os.environ` happens in the PR2 runtime):
  - [x] **NO_PROXY default**: always includes `localhost,127.0.0.1,::1` regardless of the active path; user extras are appended with de-duplication and order preservation
  - [x] **HTTPS_PROXY URL scheme**: absent for clearnet/vpn; for tor, `socks5h://127.0.0.1:<port>` (server-side DNS resolution to prevent leaks). Plain `http://` is **never used**
- [x] Implement the v1 baseline allowlist in `provider_transport_flagger.py` (PR1 / 2026-05-13):
  - [x] **Implemented the v1 baseline `KNOWN_PROVIDERS` Python dict** (6 entries, every entry flagged `unverified_baseline=True`):
    - `anthropic` / `openai` (httpx baseline, `respects_proxy=True`, `respects_socks5h=True`)
    - `gemini` (requests baseline, `respects_proxy=True`, `respects_socks5h=True`)
    - `mordred-local` (httpx baseline, `localhost_only=True`, excluded from the proxy by the NO_PROXY default)
    - `bedrock` (boto3, `respects_proxy=True`, `respects_socks5h=False`, `dns_quirk=True` — DNS leak risk with strict + tor)
    - `vertex` (google-cloud SDK, `respects_proxy="partial"`, `respects_socks5h=False`)
  - [x] Live-tested each provider's HTTPS_PROXY compliance and SOCKS5h support in Phase 0.8 verification and confirmed the dict values — **completed 2026-05-17** (see §0.8 L110-117). anthropic/openai/gemini/mordred-local were demonstrated in `tests/integration/test_provider_transport.py` and flipped to `unverified_baseline=False`. bedrock only had `respects_socks5h=False` demonstrated (deep DNS quirk deferred to v2); vertex was deferred due to being a heavy SDK — both remain `True`
  - [x] **strict mode behavior**: `evaluate(active_path, providers, policy_mode)` flags severity=`abort` when active path = `tor` + `respects_socks5h=False`; severity=`warning` when active path = `clearnet` + `respects_proxy=False`; lenient downgrades `abort` → `warning`; off produces no flags
  - [x] **user override**: entries can be added via the `overrides` arg. An override that conflicts with the baseline is rejected with `ValueError` (to prevent silently bypassing strict mode)
  - [x] `tests/test_provider_transport_flagger.py`: covers the `KNOWN_PROVIDERS` dict + policy override + strict / lenient / off matrix (18 tests)
- [x] Implement the internal Python API (`use(path)`, `status()`, `health()`, `blackout_assert()`) in `api.py` (PR1 / 2026-05-13):
  - [x] Defined the `Runtime` Protocol, with singleton management via the module-level `_RUNTIME` (wired by PR2's `set_runtime()`)
  - [x] `use(path)` raises `MordredNetworkError` on failure (including `BringupFailed`, `AlreadySwitching`, `UnknownPath`, and `PathSwitchRequiresRestart`) — silent live switching is forbidden. Registration freezes the activated route; same-route reuse is a no-op. Emitting `network.use_failed` is orchestrated by the runtime
  - [x] Implemented `blackout_assert(probe)`, with the default probe being a UDP connect to 1.1.1.1:53 (connectionless). Raises `BlackoutNotAsserted(MordredNetworkError)` if reachable — consumed by Phase 4's `keyvault.seed_display`
- [x] **M3 (transitive proxy-env failure mode)** — PR2 introduced `network.use` (`prev_path` / `new_path` / `live_subprocess_count`, best-effort `pgrep -P`; Windows returns 0). **Hardened 2026-07-27**: registration now activates and freezes the route before provider clients are built. Same-route use is a no-op; a conflicting route raises `PathSwitchRequiresRestart`, and the wizard persists it for the next Hermes process. README §M3 documents why snapshot-based provider/child transports require the restart
- [x] **M8 (transport coverage)** — PR3a / 2026-05-14:
  - [x] DNS leak defense: forces `socks5h://` on the Tor path (already in PR1) + static allowlist warnings for libraries lacking SOCKS5h support — `mordred_hermes.network.proxy_env.SOCKS5H_LIBRARY_REQUIREMENTS` + `evaluate_library_compatibility(active_path, declared_libs)` (PR3a Task #4, httpx / urllib3 / requests / aiohttp baseline, every entry `unverified_baseline=True` until PR3c)
  - [x] IPv6 caveat: added `policy.json disable_ipv6: bool` to the schema (strict default `true`, lenient/off default `false`) — reader = `mordred_hermes.network.settings.resolve_disable_ipv6` (Task #2), writer = `PolicySnapshot.disable_ipv6` (Task #7). The setting renders Tor's `ClientUseIPv6 0` only; it is not host/SDK enforcement and no longer suppresses `provider_transport_flagger._flag_for_ipv6`. Strict + Tor aborts for unverified IPv6 proxy support; lenient warns. Full kernel-level firewall enforcement is deferred to v2-N2.
  - [x] non-HTTP transport: `provider_transport_flagger.ProviderEntry.transport_class: Literal["http","tcp","udp","quic","grpc","websocket"]` + the `_flag_for_non_http` branch (PR3a Task #3; strict + tor + non-http → abort, clearnet → warning). The v1 baseline has every entry as `transport_class="http"`; raw TCP/UDP/QUIC/gRPC providers will be added via override once they appear in v2
- [x] **M9 (path failure & liveness)** — PR2 / 2026-05-14:
  - [x] Bring-up timeout: Tor 30s, VPN 10s. `register()` activates the route before provider-client construction; strict raises `MordredPathBringupFailed` + refuses process startup, lenient warns + falls back to clearnet + audits `network.bringup_failed` inside the runtime, and off is silent
  - [x] Liveness probe: an internal worker thread (30s default interval, `RuntimeConfig.liveness_interval_seconds`) runs `health()`; flips the `_dropped` flag after 2 consecutive failures (`RuntimeConfig.liveness_failure_threshold`)
  - [x] Mid-session drop: strict raises `MordredPathDropped` (block) on the next `pre_tool_call`; lenient warns + continues. Always audits `network.path_dropped` (fields `path` / `consecutive_failures`; `last_health_at` to be added in v2). The control-port version probe lands together with `stem` in PR3
- [x] Lazy-loaded subprocess management in `runtime.py` — PR2 / 2026-05-14 (`Runtime` class, `RuntimeConfig`, `State` enum, `_default_subprocess_counter`)
- [x] `pre_tool_call` hook (v1: tool-name allowlist only, per [`HOOK_PAYLOADS.md`](./HOOK_PAYLOADS.md) §4 confirming `origin_skill` is absent) — PR2 / 2026-05-14:
  - [x] **No automatic path switching (v1)** — the runtime is only a state machine, no auto-switch
  - [x] Only a single overall state check — `hooks.pre_tool_call` reads `api.is_dropped()` to refuse in strict mode
  - [x] Per-skill blocking when active path = clearnet in strict mode is already handled at **install time** (Phase 1's install_wrapper) — runtime-mismatch detection is out of scope for v1; tools do run under lenient/off
- [x] Process-scoped path lifecycle — originally shipped as `on_session_start` bring-up / `on_session_end` tear-down in PR2; **hardened 2026-07-27** so `register()` activates and freezes the route before provider clients are built, session hooks only reuse/validate it, and one `atexit` finalizer calls `Runtime.stop()` (joining the liveness worker and restoring the environment)
- [x] Bootstrap order: `hooks.wait_until_ready(timeout=5s)` remains a public helper, but registration-time activation is now authoritative and eliminates polling/hook-order dependence for initial provider transport

### 3.2 Wizard additions (Phase 3)

- [x] Added prompts to `hermes mordred configure` — **PR3a / 2026-05-14 (Task #6)**:
  - [x] Default network path (`tor` / `vpn` / `clearnet`, default `clearnet`)
  - [x] Tor binary path (default `tor`; the install path / collision-aware `tor_socks_port` are also collected together via `ask_text`)
  - [x] Mullvad account number — uses `PromptIO.ask_password` (new, prompt_toolkit `is_password=True`) to prevent shell-history leaks. The secret is written directly to `~/.hermes/.env` and never leaks to the caller via `ConfigureResult` (guaranteed absent via a dataclass tree walk in `test_secret_does_not_appear_in_returned_result`)
  - [x] Mullvad relay region (default `auto`, also supports 2-letter codes)
  - [x] Mullvad killswitch (lockdown-mode, strict-mode default `on`, lenient default `off`)
- [x] Secrets are written to `~/.hermes/.env` as `MORDRED_MULLVAD_ACCOUNT=...` — `wizard/env_file_writer.py::DotEnvFileWriter` (mode 0600, parent dir 0700, idempotent upsert, empty value → line removed, newlines / non-POSIX env names rejected with ValueError). `~/.hermes/mordred/credentials/network.json` records only an env-var REFERENCE — `wizard/credentials_writer.py::JSONCredentialsWriter` (dir 0700, file 0600, atomic write, secret-shaped values rejected with ValueError)
- [x] Implemented `hermes mordred network use <tor|vpn|clearnet>` — PR2 / 2026-05-14 (`wizard/network_cli.py::handle_use`). The command persists the desired route; if a registered runtime is already frozen on another route, the live change is refused and Hermes must be restarted. Re-selecting the active ready route is a no-op
- [x] Implemented `hermes mordred network status` (shows active path / health / last liveness result / lockdown state) — PR2 / 2026-05-14 (`wizard/network_cli.py::handle_status`; live state when a runtime is present, falls back to the disk config otherwise. Lockdown state to be added in v2)

### 3.3 Tests (Phase 3)

- [x] `tests/test_paths.py`: path manager state machine — landed as the `tests/test_network_runtime_*.py` suite (PR2 / 2026-05-14; split by basic/Tor/VPN/switching/resilience/recovery/synchronization/API responsibility on 2026-07-28)
- [x] `tests/test_proxy_env.py`: env vars are set correctly — PR1 (2026-05-13)
- [x] `tests/integration/test_tor.py` (docker-compose with a Tor container; asserts SOCKS5 reachability) — **PR3b completed (2026-05-14)**: 3 tests (SOCKS5 handshake / socks5h DNS roundtrip via check.torproject.org / proxy_env HTTPS_PROXY round-trip via httpx). alpine + tor + loopback-only port binding + RFC1918 SocksPolicy. A 3-tier skip guard (env opt-out / OS / binary + daemon probe) auto-skips on a dev box / macOS runner / no-docker CI. The `.github/workflows/ci.yml` `integration-tor` job runs on ubuntu-24.04 by default
- [x] `tests/integration/test_vpn.py` (gated by `MORDRED_LIVE_VPN_TEST=1`): real Mullvad connection — **PR3b completed (2026-05-14)**: 3 tests (bring-up roundtrip / lockdown rollback when `lockdown_applied_by_us` / wg show handshake freshness under 180s). Requires both `MORDRED_LIVE_VPN_TEST=1` and `MORDRED_MULLVAD_ACCOUNT` (a 16-digit account number from a repo secret). workflow_dispatch only — the operator triggers it manually via `.github/workflows/integration-vpn.yml`
- [x] Privacy-check coordination (v1): end-to-end test of the **install-time** path (the `hermes mordred install` wrapper) for a skill declaring `network_requirements: tor` — **completed in the 2026-05-17 stocktake**: install-time coordination E2E is covered by `tests/test_install_dispatch.py::TestRunBlock` (clearnet → block + audit) + `TestRunAllow::test_tor_skill_in_strict_mode` (`tests/fixtures/tor_skill/SKILL.md`'s `metadata.mordred.network_requirements: tor` → allow). Live Wireshark verification is out of scope for v1 (unnecessary since there's no auto-routing). ~~runtime auto-switch~~ is out of scope for v1 (absent `origin_skill`, pending v2-H2). Users select Tor with `hermes mordred network use tor` and restart Hermes so the process route is activated before clients — confirmable via `policy explain`

### Acceptance gate (Phase 3)

> **PR2 (2026-05-14) status**: all code paths in place, unit tests green. Live verification (real Tor circuit, real Mullvad tunnel, each bundled provider through HTTPS_PROXY) will be done in PR3 — check off these 4 items at that point.

- [x] Confirmed that a skill with `network_requirements: tor` works over a **Tor-configured process route** (`hermes mordred network use tor`, then restart) — **completed 2026-05-17** (approach C): since Hermes has no skill-invoke API (a skill is a Markdown instruction document, not callable code — `tools/skills_tool.py` only returns the SKILL.md string), `tests/fixtures/tor_skill/` bundles an executable counterpart to SKILL.md, `network_probe.py`. `tests/integration/test_tor.py::TestTorSkillEndToEnd` runs the probe after applying the configured Tor env (`proxy_env.desired_env`) and asserts `IsTor=True` (live-verified via the `integration-tor` CI job). Registration/runtime coverage separately verifies pre-client activation, same-route reuse, and conflicting-route restart refusal. Hermetic response-handling is covered by `tests/test_tor_skill_fixture.py` (6 tests, httpx MockTransport). ~~auto-routing at tool-call time~~ is out of scope for v1 (Phase 0.8 verification confirmed `origin_skill` is absent; planned to return in v2-H2)
- [x] ~~**[infra-blocked]** Manual `hermes mordred network use vpn` switches path within 2s~~ — **superseded 2026-07-27**. A live cross-route switch is intentionally refused after registration because provider clients captured the prior transport. The command persists VPN for the next Hermes process; `tests/integration/test_vpn.py` remains `MORDRED_LIVE_VPN_TEST=1` gated for actual Mullvad bring-up verification
- [x] `mordred_network.api.status()` returns truthful state — **code complete** (truthfulness asserted in `tests/test_network_runtime_basic.py::TestInitialState` / `tests/test_network_runtime_tor.py::TestTorUse::test_status_reports_tor_active` and many others). **Completed in the 2026-05-17 stocktake**: cross-checked code-complete against the actual code; since `status()` is a pure state reader, no separate live verification is needed (transport live verification was separately completed in §0.8 L110-117 = PR #41)
- [x] All bundled provider plugins continue to function under each path — **completed 2026-05-17** (§0.8 L110-117 landed in PR #41; `tests/integration/test_provider_transport.py` demonstrates anthropic / openai / gemini / mordred-local transport over HTTPS_PROXY using the in-process SOCKS5 inspector, flipped to `unverified_baseline=False`). Deep verification of bedrock / vertex is deferred to v2 (see §0.8 L115-116 for the reasons)

---

## Phase 4 — Key Management (`mordred_keyvault`)

### Open decisions

- [x] **[DECIDE confirmed]** DECIDE: implement the PC↔phone pairing flow (QR + mDNS + self-signed-TLS) in v1, or defer it to v2-F7 → **confirmed 2026-06-03: deferred to v2-F7** (operator-approved). It represents roughly a week of additional implementation including phone-side UI selection, so it's out of scope for v1 and does not block v1 GA. v1 keeps the degraded flow (displaying both halves on the PC) as specified in SPEC (since this weakens the UX-level safety guarantee, v2-F7 is a candidate for early promotion). See ROADMAP.md §v2-F7 for details
- [x] DECIDE: how to handle the pre-Phase-4 plaintext audit log — provide a manual purge (`hermes mordred audit purge --before YYYY-MM-DD --yes`) → **confirmed and implemented 2026-05-17** (the `audit purge` CLI landed in Phase 4 PR8, see §4.2 L443. Checkbox corrected to `[x]` since the decision's disposition is now satisfied by the implementation)

### 4.1 `mordred_keyvault` plugin

> **Phase 4 PR2 completed** (2026-05-14): pure-Python primitives `digest.py` / `backup.py` / `recovery.py` landed. Incorporated Codex review feedback (12 items: BLOCKER×1 + HIGH×3 + MEDIUM×5 + LOW×2 + NIT×1): froze the SPEC.md digest formula's pseudocode + fixed test vector (BLOCKER #1), added a `b"MRKV"` magic + version + KDF params + AAD binding to the backup wire format (#2, #3), verify-before-decrypt ordering in recovery (#4), separated BackupCorrupt from InvalidTag (#5), a digest length-confusion guard (#6), limited the reason-enum freeze to only the 2 codes with a PR2 emit site (#8), audit sink shape is a `Callable[[dict], None]` per POLICY.md §Audit entry shape (#9), deterministic salt-freshness checking via RNG monkeypatching (#11). Also rejects a KDF cost-parameter DoS found during integration testing (`m_cost` MSB flip → a 16 GiB allocation request) in parse_header. 70 backup + recovery tests passed in <2s, all 819 mordred-hermes tests green. The Phase 4 acceptance gate is **not yet met** (api.py / native.py / wrap.py / seed_display / log_encryption are still stubs, to be wired up in the following PR3/PR4).

> **Phase 4 PR4 step-0 doc freeze** (2026-05-15): froze the contract reflecting the pre-implementation Codex review (BLOCKER × 3 + HIGH × 5 + MEDIUM × 3 + LOW × 1) of the `api.py` public Python surface, in SPEC.md §"PR4 API contract & MREN envelope wire format" / POLICY.md §"Phase 4 PR4 step-0 freeze" / PATHS.md §"Expected substructure". Key decisions (codex-corrected): (a) `generate` becomes **two-phase** — `prepare_generate(seed, passphrase, pow) -> (SeedDisplayHandle, expected_digest)` only computes the digest in memory; only after the user confirms via an offline channel does `confirm_generate(handle, user_digest)` perform the Keychain + meta.json mutation; on mismatch, zero state changes occur and `keyvault.init_denied` is emitted (BLOCKER #2 fix). (b) `SeedDisplayHandle` is an opaque class with `__repr__` redacted / `__eq__` forbidden / `__hash__=None` / `consume()` zero-fills the internal bytearray (BLOCKER #3 fix). (c) normalization is **split** — `_normalize_seed_phrase = NFKD + casefold + whitespace-collapse` (BIP39 word-list), `_normalize_passphrase = NFKD only` (preserves entropy; BIP39 reference) (HIGH #1 fix). (d) `decrypt(key_id, envelope_id, purpose)` requires a caller-supplied `purpose`, checked against the envelope's `purpose_hash` via `hmac.compare_digest` (cross-purpose replay defense, HIGH #2 fix). (e) storage is **managed** — `encrypt` returns an `envelope_id`; the `.gcm` file is persisted via `os.open(O_NOFOLLOW) + tmp+fsync+rename+fsync(parent_dir)` + `fcntl.flock(.lock)`, with mode `0600`/`0700` verified via fstat (HIGH #3/#4 fix). (f) `export_backup` / `import_backup` use a full **ciphertext-rewrap manifest** — each DEK is re-wrapped under the Argon2id-KEK and stored in a manifest inside the MRKV blob; on recovery, each DEK is re-wrapped under the new Enclave key (BLOCKER #1 fix, satisfying acceptance gate L437). (g) froze 4 new audit codes (`keyvault.init_started` #21 / `init_completed` #22 / `init_denied` #23 / `backup_exported` #24) in POLICY.md; their addition to `_audit_reasons.py:ReasonCode` lands in step-D. (h) when the `is_secure_enclave_available()` probe returns False, **fail** the live test under `MORDRED_KEYVAULT_LIVE=1` (HIGH #5, preventing a silent skip). (i) froze the MREN envelope wire format in SPEC (196+N bytes, AAD = `magic ‖ version ‖ key_id_hash ‖ purpose_hash ‖ wrapped_dek`, 164 bytes). (j) simultaneously fixed the wrap-surface drift in SPEC L501-505 (removed `audit_sink` from `generate_wrapping_key`, added `backend` to every function) (MEDIUM #1). step-0 is a docs-only commit; implementation follows in steps A/B/C/D/E/F/G (PR4 overall is estimated at ~32-42h of core dev plus Codex review iterations, roughly 5-8 working days). PR4 satisfies acceptance-gate items L436 (encrypt/decrypt roundtrip) / L437 (backup → wipe → restore → decrypt roundtrip) / L439 (digest mismatch reject), but L435 (`requires_keyvault` install block) / L438 (Seed display blackout check) / L440 (`mordred_network`-absent fallback) / L441 (audit log encryption) remain for PR5 onward — Phase 4 as a whole is not completed by PR4 alone (codex LOW #1).

> **Phase 4 PR3 completed** (2026-05-14): the Secure-Enclave wrap/unwrap seam (`native.py` + `wrap.py` + `_exceptions.py`) landed. Incorporated Codex review feedback (2 BLOCKER + 4 HIGH + 4 MEDIUM + 2 LOW + 1 NIT): changed the audit codes to just 2 (`keyvault.unwrap_authorized` / `keyvault.unwrap_denied`) (BLOCKER #1: the authorization boundary is **unwrap only** — wrap is an offline operation using the Enclave's **public** key + a software ephemeral private key), removed `kw_iv(8)` from the wire format, settling on 127 bytes (BLOCKER #2: RFC 3394 AES-KW takes a 32-byte input → 40-byte output, no separate IV needed), the KDF is a single stage of raw P-256 ECDH + HKDF-SHA256 (HIGH #1), the HKDF `info` binds `magic ‖ version ‖ alg_suite ‖ key_id_hash ‖ ephemeral_pub` to obtain AAD-equivalent integrity (HIGH #2), CI can't satisfy a biometric prompt so the live test is pinned to dev-only (HIGH #4), withdrew the Apple-Silicon-only assumption and covered T2 Intel Macs too via a capability probe (MEDIUM #1), fixed access control to `.biometryCurrentSet | .privateKeyUsage` + `.whenPasscodeSetThisDeviceOnly` (MEDIUM #2), the NativeBackend Protocol abstracts only Keychain/SecKey operations, so HKDF/AES-KW/wire parsing are tested with real crypto (MEDIUM #4), split exceptions into `WrapError` + 5 sibling subclasses (NIT #1), froze in SPEC the internal surface that PR4 calls (LOW #2). native (11 tests) + wrap (50 tests; FakeBackend = a software P-256 keypair) totaling 61 tests passed, all 891 mordred-hermes tests green, ruff + format + mypy --strict all clean. The production `_SecKeyBackend` (pyobjc bridge) and the live integration test (`tests/integration/test_keyvault_macos.py`) are **deferred to PR4** (landing in the same PR as api.py; PR3 covers only the contract + the Fake-tested surface).

> **Phase 4 PR4 completed** (2026-05-15 to 16): the `api.py` public surface (`prepare_generate` / `confirm_generate` / `generate` / `encrypt` / `decrypt` / `verify_digest` / `SeedDisplayHandle`) + the `_storage.py` managed storage layer landed across 4 sub-PRs (#26 pr4-api-and-seckey / #27 pr4b-storage-envelope / #28 pr4c-generate-backup / #29 pr3-review-low3). Incorporated several rounds of Codex pre-merge review (making `confirm_generate` a pure reader + reordering the transaction, `expected_digest` expiry semantics, generate-handle wipe, `consume()` thread-safety, blocking seed leaks via `__getstate__`/`__setstate__`, post-coercion digest byte-length, etc.). All 1211 mordred-hermes tests green, ruff + format + mypy --strict clean. **Not yet done**: wiring `api.export_backup` / `import_backup` into api.py (the lower-level primitives already landed in PR2), `seed_display.py` / `network_fallback.py` / `log_encryption.py` (still stubs), the §4.2 wizard CLI, §4.3 integration tests, and Phase 4 acceptance gate L436-443.

> **Phase 4 PR4 step-E completed** (2026-05-16): `api.export_backup` / `api.import_backup` (the ciphertext-rewrap manifest) landed — satisfying Phase 4 acceptance gate L437 (backup → wipe → restore → decrypt roundtrip) / L439 (digest mismatch reject). Since the Enclave wrapping key is non-exportable, cross-machine recovery is otherwise impossible (Codex BLOCKER #1) — so export unwraps each envelope's DEK and re-encrypts the plaintext under a **portable** manifest AAD (`MRMN ‖ key_id_hash ‖ purpose_hash`, which excludes the per-device MRKW prefix), packs the DEK + portable ciphertext into a canonical-JSON manifest, and wraps it in the PR2 MRKV blob (the Argon2id-KEK protects the manifest at rest, with a verification digest embedded). Import does verify-before-decrypt (a mismatch between the recomputed and embedded digest raises `RecoveryDigestMismatch` before any Enclave key is generated), then generates the new device's Enclave key and re-wraps each DEK → reconstructs the MREN envelope; on failure, rolls back the ciphertext tree + Enclave key + meta row. Split the internal `_encode_envelope` / `_parse_envelope` into a hash-input core (`_encode_envelope_from_hashes` / `_split_envelope`) reused by the import path (the cleartext purpose can't be recovered from the stored envelope, only the purpose_hash). Added audit code #24 `keyvault.backup_exported` to the ReasonCode freeze (event `keyvault.backup_export`; the success-path emit uses `contextlib.suppress`, freeze count now 24). 16 backup tests + all 1226 mordred-hermes tests green, ruff + format + `mypy --strict src` all clean. The production `_SecKeyBackend` (pyobjc) and the live integration test remain for later; the `seed_display.py` / `log_encryption.py` / `network_fallback.py` stubs are still untouched.

> **Phase 4 PR5 completed** (2026-05-16): `network_fallback.py` landed — a prerequisite for `keyvault.seed_display`'s (PR7) network blackout check. Implements the **fallback** path from SPEC.md §Seed phrase display security: when `mordred_network` is absent, keyvault probes reachability directly via macOS `SCNetworkReachability` (pyobjc). `resolve_blackout_assert()` is the single entry point — when `mordred_network` is importable, it delegates to `network.api.blackout_assert` and translates the exception into keyvault-owned `BlackoutNotAsserted`; when absent, it returns the OS-API `blackout_assert`. `blackout_assert` is **fail-closed**: when the probe can't run (non-macOS / pyobjc absent → `NetworkFallbackUnavailable`), it raises `BlackoutNotAsserted` to refuse the Seed display (never lets the display proceed on an unprobeable host). The module is importable on every platform (pyobjc is a call-time lazy import, the same contract as `keyvault.native`). `_interpret_reachability_flags` follows the standard Apple interpretation (`Reachable` set and `ConnectionRequired` clear). Added `pyobjc-framework-SystemConfiguration>=10.0` to the `pyproject.toml [macos]` extra. No new audit codes (the blackout-decision audit is done by the caller, seed_display, in PR7). 27 network_fallback tests + all 1253 mordred-hermes tests green, ruff + format + `mypy --strict` clean. **Partially satisfies** Phase 4 acceptance gate L454 (fallback in a `mordred_network`-absent env) — wiring into seed_display and the `keyvault init` CLI is PR7/PR8.

> **Phase 4 PR6 completed** (2026-05-16): `log_encryption.py` landed — an AES-GCM encryption layer that slots into the audit `Writer` Protocol frozen in Phase 1. Implements the `EncryptedWriter` from SPEC.md §Audit-log encryption coupling. A line-oriented `MRAL` v1 wire format (line 0 = a JSON header `{"fmt","ver","key_id","wdek"}`, lines 1+ = `base64(nonce(12) ‖ AES-GCM-ciphertext ‖ tag(16))`) — one entry per line, preserving `O_APPEND`'s whole-entry atomicity (Writer invariant #2) while avoiding a full-file re-encryption. The audit-log DEK is stored in the header's `wdek` field as a 127-byte `MRKW` blob wrapped via `wrap.wrap_dek` (offline, using the Enclave public key, no prompt) — **only the wrapped DEK ever touches disk**; the plaintext 32-byte DEK exists only in the writer's memory and its reference is dropped on `close()`. The DEK is lazily generated on the first append (fresh per file). The per-entry AES-GCM AAD = `MAGIC ‖ version ‖ SHA-256(header line)`, binding each entry to the file header — splicing entries from a different file, or replaying after tampering with the header, fails the tag check. `decrypt_log_file` unwraps the DEK via `wrap.unwrap_dek` (the Secure Enclave authorization boundary, emitting `keyvault.unwrap_authorized`), transparently handles gzip-rotated files, and raises `AuditLogDecryptError` on structural/integrity errors, propagating `WrapAuthCancelled`/`WrapKeyNotFound` so the CLI can distinguish them. Rotation matches the Phase 1 NDJSONWriter (daily + size cap + gzip + 30-day retention, with a fresh file+DEK+header on each rotation); pre-existing foreign files (e.g., the pre-Phase-4 plaintext log) are rotated aside rather than overwritten. Complies with Writer invariants #1/#3 via millisecond-precision `ts` injection + `0600`/`0700` modes, and a `TYPE_CHECKING` conformance shim lets mypy catch Writer Protocol drift. No new audit codes (the unwrap audit is emitted by `wrap.unwrap_dek`). 24 log_encryption tests + all 1279 mordred-hermes tests green, ruff + format + `mypy --strict src` clean. **Partially satisfies** Phase 4 acceptance gate L457 (AES-GCM encryption of the audit log) — wiring the privacy_check factory swap and the `hermes mordred audit decrypt` CLI is PR8.

> **Phase 4 PR7 completed** (2026-05-16): `seed_display.py` landed — the orchestrator for the Seed phrase display flow, implementing SPEC.md §Seed phrase display security. `display_seed(handle, surface, ...)` runs 6 steps: (1) the network blackout assert (`network_fallback.resolve_blackout_assert`, **fail-closed** — if the host is reachable, raises without even showing the banner); (2) the M4/M5 warning banner (`SEED_DISPLAY_BANNER` — spelling out physically disconnecting Wi-Fi/Ethernet/Bluetooth/USB tether/hotspot, and stopping any screen recorder/remote desktop); (3) a screenshot pre-check; (4) obtaining the seed via a one-shot `SeedDisplayHandle.consume()` (`SeedDisplayExpired` propagates); (5) a `time.monotonic()`-based 60s timer + capture polling; (6) auto-clear in `finally` (clears the surface on every exit path). Screenshot detection (M5) is best-effort: `_default_capture_probe` polls macOS Quartz's `CGScreenIsBeingCaptured`; on detection it immediately clears the surface → emits the `keyvault.seed_display_aborted_screenshot` audit entry → raises `SeedDisplayAborted` (carrying a `detector` attribute, with any audit-sink failure chained via `__context__`). The probe is **fail-open** (non-macOS / pyobjc absent / a bridge error all yield `None` — the opposite of the blackout assert's fail-closed behavior, since screenshot detection is advisory and the banner is the primary defense). The `SeedDisplaySurface` Protocol (banner/show/clear) abstracts rendering, with PR8's `keyvault init` CLI supplying the surface implementation. `SeedDisplayHandle` stays in api.py (not relocated, so as not to break api.py's consumers); the Quartz bridge is a call-time lazy import so the module remains importable on every platform. No new audit codes (`keyvault.seed_display_aborted_screenshot` was already frozen in the PR2 step-0 ReasonCode freeze). Added `pyobjc-framework-Quartz>=10.0` to the `pyproject.toml [macos]` extra. Incorporated code-review feedback (M1/L1/L2/L3): `poll_interval <= 0` fails fast with `ValueError`, a pre-display abort consumes the handle and zero-fills the seed payload, and the docstring now spells out the main-display probe's limitations and the independence of the TTL constant. 22 seed_display tests + all 1277 mordred-hermes tests green, ruff + format + `mypy --strict src` clean. Wiring from the `keyvault init` CLI is PR8.

> **Phase 4 PR9 completed** (2026-05-16): the production `_SecKeyBackend` (pyobjc Secure-Enclave backend) landed in `keyvault/_seckey_backend.py` — the production implementation of the `NativeBackend` Protocol frozen in PR3. The design follows `native.py`'s narrow-boundary style, split into 2 layers: (1) the `_SecKeyOps` Protocol = the minimal pyobjc-touching surface (create / copy-public / delete / ECDH), where each method returns plain `bytes` or raises an `_OpsError` carrying a translated `OSStatus`/`LAError` — `Security.framework` types never cross the boundary. (2) `_SecKeyBackend` = the flow + error-translation logic (building `kSecAttrApplicationTag = b"mordred-hermes.wrap." + key_id_hash`, mapping `_OpsError` into the frozen `WrapError`/`NativeBackendError` taxonomy). The production ops (`_PyobjcSecKeyOps`) call `SecKeyCreateRandomKey` (token=SecureEnclave, access control `.privateKeyUsage | .biometryCurrentSet` + `.whenPasscodeSetThisDeviceOnly`, per SPEC.md §Wrap §Access-control attributes) / `SecItemCopyMatching` / `SecItemDelete` (idempotent, with errSecItemNotFound=success) / `SecKeyCopyKeyExchangeResult` (`ECDHKeyExchangeStandard`, the sole authorization boundary). OSStatus/LAError translate through a frozen 5-code translation table (`_translate_error`, with unknown codes conservatively mapped to `auth_failed`). Replaced `native._probe_secure_enclave_capability`'s `return False` stub with `_seckey_backend.probe_capability()` (a `.privateKeyUsage`-only generate-then-delete, no prompt). Cross-platform unit tests inject a software-crypto `_FakeOps` (real `cryptography` P-256) into `_SecKeyOps`, covering the flow + translation + wrap/unwrap roundtrip (39 tests). The live `tests/integration/test_keyvault_macos.py` (gated by `MORDRED_KEYVAULT_LIVE=1`, §4.3 L451) verifies generate→wrap→unwrap (biometric prompt)→delete against a real Secure Enclave, and fails rather than skips when the gate is ON but no Enclave is present (SPEC HIGH-5). 1381 tests passed / 7 skipped, ruff + format + `mypy --strict src` clean. **Deferred to PR10**: wiring the §4.2 `keyvault init` / `recover` / `audit decrypt` CLI commands (injecting the production backend into api/log_encryption), and Phase 4 acceptance gate L458-462.

> **Phase 4 PR10 completed** (2026-05-16): the 3 backend-coupled CLI commands deferred by PR9 (`keyvault init` / `keyvault recover` / `audit decrypt`) + the L465 encrypted-audit-log factory swap landed. step-0 (docs): froze the PoW algorithm (a seed-bound leading-zero-bits BLAKE3 counter search, `MRPOW\x01` prefix, `POW_DIFFICULTY_BITS=20` baseline, 2 fixed test vectors) and the `keyvault init` flow in SPEC.md. step-A: `keyvault/pow.py` (`compute_pow`) + `keyvault/_bip39.py` (24-word BIP39, 256-bit only, checksum verification) + vendored the official BIP39 English wordlist (`_bip39_wordlist.txt`, SHA-256 `2f5eed53…dbda`). step-B: `audit_cli.decrypt` — decrypts rotated `audit.log.<date>[.N][.gz]` files plus the current day's active log via `log_encryption.decrypt_log_file`, distinguishing denied prompt / missing key / corrupt cases. step-C: `keyvault_cli.recover` — reads the blob + prompts for Seed/Passphrase + pre-validates the BIP39 checksum + recomputes the seed-bound PoW + calls `api.import_backup`. step-D: `keyvault_cli.init_keyvault` + `TerminalSeedSurface` — re-init guard / double Passphrase prompt / BIP39 generation / PoW / `prepare_generate` → `display_seed` (network blackout) → offline digest confirmation → `confirm_generate`. step-E (L465): `privacy_check.audit.make_audit_writer` — selects `EncryptedWriter` when keyvault is initialized and the audit-log wrapping key is usable, otherwise fails open to `NDJSONWriter`. `_runtime._load_state` now goes through the factory. `init_keyvault` also provisions the `mordred.audit-log` wrapping key. step-F: integration test L453 (added an AES-GCM encrypt/decrypt roundtrip to `test_keyvault_macos.py`) + L454 (`tests/integration/test_network_fallback.py`). Replaced cli.py's 3 stubs (`NotImplementedError`) with real handlers, and removed the old `test_wizard_cli` stub-deferral test. All mordred-hermes tests green, ruff + format + `mypy --strict src` (64 files) clean. Achieves Phase 4 acceptance gate L460 (encrypt/decrypt through the Enclave) / L461 (backup roundtrip) / L463 (digest mismatch reject) / L464 (network-absent fallback) / L465 (audit log encryption).

- [x] Added `macos = ["pyobjc-framework-Security>=10.0", "argon2-cffi>=23", "cryptography>=42", "blake3>=0.4"]` to `pyproject.toml`'s `[project.optional-dependencies]` — completed around PR1 (#21)
- [x] `native.py`: a `Security.framework` wrapper (lazy import, preventing `ImportError` on non-macOS) — landed in Phase 4 PR3 (this PR). `_lazy_import_security()` is cached; on non-Darwin it short-circuits by raising `WrapNativeUnavailable`; on macOS with pyobjc absent it chains an `ImportError` via `__cause__`. `is_secure_enclave_available()` uses a capability probe (try-generate-and-delete) so it covers T2 Intel Macs too, without depending on `platform.machine()` (Codex MEDIUM #1). The production `_SecKeyBackend` (real pyobjc calls) lands together with api.py in PR4
- [x] `api.py` key-generation surface: two-phase `prepare_generate` / `confirm_generate` + a compatible `generate`, an opaque `SeedDisplayHandle` (`__repr__` redacted / `__eq__` forbidden / `consume()` zero-fills the internal bytearray), `verify_digest`, and the split BIP39 Unicode normalization (`_normalize_seed_phrase` = NFKD+casefold+whitespace-collapse / `_normalize_passphrase` = NFKD only) — landed in Phase 4 PR4 step-A/D (PR #26, #28)
- [x] `api.py` envelope surface: `encrypt` (returns an `envelope_id`) / `decrypt` (checks the caller-supplied `purpose` against the envelope's `purpose_hash` via `hmac.compare_digest`, defending against cross-purpose replay) — landed in Phase 4 PR4 step-B/C (PR #26, #27). The internal surface (`generate_wrapping_key` / `get_wrapping_key_public` / `delete_wrapping_key` / `wrap_dek` / `unwrap_dek` + the `WrapError` 6-class taxonomy) was already frozen in PR3 (Codex LOW #2)
- [x] `_storage.py`: the managed storage layer — `ensure_layout` / `atomic_write` (`os.open(O_NOFOLLOW)` + tmp+fsync+rename+fsync(parent_dir)) / `safe_read` / `keyvault_lock` (`fcntl.flock`) / `load_meta` / `save_meta`, with mode `0600`/`0700` verified via fstat — landed in Phase 4 PR4 step-B (PR #27) (HIGH #3/#4 fix)
- [x] Implemented `api.py`'s `export_backup` / `import_backup` (the api-level ciphertext-rewrap manifest) — landed in Phase 4 PR4 step-E. Export unwraps each envelope's DEK → re-encrypts it under the portable manifest AAD (`MRMN ‖ key_id_hash ‖ purpose_hash`, without the per-device MRKW prefix) → packs the manifest into the PR2 MRKV blob (Argon2id-KEK); import does verify-before-decrypt, then re-wraps each DEK under the new Enclave key → reconstructs the MREN envelope. Added the `keyvault.backup_exported` audit code (#24) to the ReasonCode freeze (count 24). The lower-level primitives (`backup.py` / `recovery.import_backup`) already landed in PR2
- [x] `crypto.py`: AES-GCM encrypt/decrypt (via the cryptography library) — landed in Phase 4 PR1 (#21), supporting AES-128/192/256, with the nonce from `secrets.token_bytes(12)`
- [x] `wrap.py`: Secure Enclave-backed wrapping-key integration — landed in Phase 4 PR3 (this PR). A 127-byte blob (`MRKW(4)|version(1)|alg_suite(1)|key_id_hash(16)|ephemeral_pub(65)|wrapped_dek(40)`), using raw P-256 ECDH (`SecKeyCopyKeyExchangeResult` with `kSecKeyAlgorithmECDHKeyExchangeStandard`) + HKDF-SHA256 (the `info` binds the non-secret fields → AAD-equivalent integrity, Codex HIGH #2) + AES-KW (RFC 3394, using only the built-in AIV; the `kw_iv` field was removed, Codex BLOCKER #2). Wrap is offline (via the Enclave public key, no prompt, no audit); unwrap is authorized (`enclave_ecdh` triggers a biometric prompt via `SecKeyCopyKeyExchangeResult`, emitting `keyvault.unwrap_authorized` / `keyvault.unwrap_denied`; `native_error_code` translates to `user_cancelled` / `auth_failed` / `biometry_lockout` / `passcode_not_set` / `key_not_found`, with the raw `OSStatus` never surfaced in the audit). Audit-sink exception chaining follows the PR2 `recovery._emit_mismatch` pattern (`except Exception`, attaching `sink_exc` to `__context__`, with the raise done outside the `except` block to avoid Python's implicit `__context__` overwrite). The NativeBackend Protocol abstracts only Keychain/SecKey operations (Codex MEDIUM #4); FakeBackend (software P-256 via `cryptography`) exercises 50 tests with real crypto
- [x] `backup.py`: an Argon2id (`m=46 MiB, t=1, p=1`) wrapped backup blob, embedding a 16-byte salt + verification digest in the blob — landed in Phase 4 PR2 (this PR). Wire format: `b"MRKV"` magic + version(1) + kdf_id(Argon2id) + m/t/p_cost (uint32 BE) + salt(16) + digest(32) + aes_blob_len(4 BE) + aes_blob. AAD = magic ‖ version ‖ kdf_id ‖ KDF params ‖ salt ‖ digest (66 bytes). Added a DoS guard to parse_header (m_cost ≤ 1 GiB / t_cost ≤ 64 / p_cost ≤ 16)
- [x] `recovery.py`: digest recomputation + mismatch rejection in `import_backup` — landed in Phase 4 PR2 (this PR). Verify-before-decrypt ordering (parse_header → 32-byte length guard → `hmac.compare_digest` → decrypt_body). `audit_sink: Callable[[dict], None]` emits the POLICY.md `keyvault.recovery_digest_mismatch` entry
- [x] `digest.py`: `digest = hash(hash(SeedPhrase), hash(Passphrase) ⊕ top4(PoW))` (BLAKE3-based) — landed in Phase 4 PR2 (this PR). Added the pseudocode + fixed test vector to SPEC.md §Key generation and verification digest (Codex BLOCKER #1). top4(PoW) is `PoW_bytes[:4]`; the XOR applies only to `pass_hash[:4]`, with the remaining 28 bytes passed through. `verify_digest` does a timing-safe compare via `hmac.compare_digest` + a 32-byte length-confusion guard
- [x] `seed_display.py`: the Seed display flow (blackout assert → a 60-second timer based on `time.monotonic()` → display → auto-clear) — landed in Phase 4 PR7. `display_seed(handle, surface, ...)` is the orchestrator: blackout assert (`network_fallback.resolve_blackout_assert`, fail-closed) → the M4 banner → screenshot pre-check → obtaining the seed via `SeedDisplayHandle.consume()` → the 60s monotonic timer + capture polling → auto-clear in `finally`. The `SeedDisplaySurface` Protocol (banner/show/clear) abstracts rendering, with PR8's `keyvault init` CLI supplying the surface
- [x] **M5 (capture caveats)**: implemented macOS `CGDisplayRegisterReconfigurationCallback` + `CGScreenIsBeingCaptured` polling in `seed_display.py`; on detection, immediately clears the Seed display + audits `keyvault.seed_display_aborted_screenshot` (added at the same time as the Phase 4 reason-enum freeze). Detecting screen recording / VNC / Loom is out of scope for v1, only warned about via the startup banner — landed in Phase 4 PR7. The v1 detector is `CGScreenIsBeingCaptured` polling (`_default_capture_probe`, detector value `cg_screen_is_being_captured`). On detection, the surface is cleared immediately → an audit entry is emitted (`event=keyvault.seed_display` / `decision=block` / `detector` field) → raises `SeedDisplayAborted`. The probe is **fail-open** (non-macOS / pyobjc absent / a bridge error all yield `None`, since screenshot detection is best-effort — the opposite of the blackout assert's fail-closed behavior). The `CGDisplayRegisterReconfigurationCallback` callback approach was not adopted in v1 (polling was used instead); the audit `detector` enum also allows `cg_display_reconfiguration` for future use
- [x] **M4 (blackout caveat)**: the pre-display banner in `seed_display.py` explicitly tells the user "disconnect Bluetooth / USB tether / hotspot — `blackout_assert()` only detects the standard OS network stack" — landed in Phase 4 PR7 (the `SEED_DISPLAY_BANNER` constant, shown via `surface.banner()` before display. Also warns about physical air-gapping / stopping screen recorders / remote desktop)
- [x] `network_fallback.py`: a fallback to a direct OS-API call when `mordred_network.api.blackout_assert` is absent — landed in Phase 4 PR5. `resolve_blackout_assert()` is the single entry point: when `mordred_network` is importable, it delegates to `network.api.blackout_assert` (translating the exception into keyvault-owned `BlackoutNotAsserted`); when absent, it falls back to an OS-API probe via macOS `SCNetworkReachability` (via pyobjc, a lazy import so the module stays importable on every platform). `blackout_assert` is **fail-closed** — when the probe can't run (non-macOS / pyobjc absent), it raises `BlackoutNotAsserted` to refuse the Seed display. Added `pyobjc-framework-SystemConfiguration>=10.0` to the `pyproject.toml [macos]` extra. 27 network_fallback tests + all 1253 mordred-hermes tests green, ruff + format + `mypy --strict` clean. Wiring from seed_display (PR7) follows later
- [x] `log_encryption.py`: an AES-GCM encryption layer slotting into the Phase 1 audit `Writer` interface — landed in Phase 4 PR6. `EncryptedWriter` (implementing the Phase 1 `Writer` Protocol) encrypts new entries with line-oriented AES-GCM (the `MRAL` v1 wire format, 1 entry = 1 base64 line); the audit-log DEK is wrapped via `wrap.wrap_dek` and stored in the header's `wdek` (the plaintext DEK stays memory-only). The per-entry AAD binds the header's SHA-256, blocking cross-file entry splicing. `decrypt_log_file` decrypts via `wrap.unwrap_dek` (Secure Enclave authorization, emitting `keyvault.unwrap_authorized`), transparently handling gzip-rotated files too. Frozen in SPEC.md §"Encrypted audit-log wire format (`MRAL` v1)". 24 log_encryption tests + all 1279 mordred-hermes tests green, ruff + format + `mypy --strict src` clean. No new audit codes. Wiring the privacy_check factory swap and the `audit decrypt` CLI follows later (PR8)
- [x] Skill opt-in enforcement: wired up `requires_keyvault: true` in Phase 4, which was a metadata-read no-op in Phase 1 — **completed 2026-05-16** (§4.1). Added `requires_keyvault` / `keyvault_initialized` arguments to `evaluate_install` (strict=block `policy.strict.keyvault_uninitialized` / lenient=warn `policy.lenient.keyvault_uninitialized_warning` / off=allow). The keyvault-initialized check uses the backend-free probe `privacy_check/_keyvault_probe.py` (only reads `meta.json`, lazily imports `keyvault._storage`, works on every platform), wired in via `install_wrapper.run`'s `keyvault_probe`. Skills that don't declare `requires_keyvault` never invoke the probe at all. The network-level block short-circuits before the keyvault check. The reason-enum freeze grew from 24 to 26 (POLICY.md §"Phase 4 §4.1 freeze"). `policy_explainer` was updated to accept the same argument. 1338 tests green, ruff + format + `mypy --strict src` clean

### 4.2 Wizard additions (Phase 4)

> **Phase 4 PR8 completed** (2026-05-16): the 3 backend-free CLI commands landed, implementing `hermes mordred {keyvault list, keyvault verify-digest, audit purge}`. The new `wizard/keyvault_cli.py` (`list_keys` / `verify_digest` + `cli_*` adapters) imports only `keyvault._storage` (pure stdlib) and has no dependency on `cryptography` / `NativeBackend` — importable on every platform. Added `purge` to `audit_cli.py`. The remaining 3 commands (`keyvault init` / `keyvault recover` / `audit decrypt`) are deferred to a separate PR since the production Secure-Enclave `NativeBackend` (`_SecKeyBackend`) isn't implemented yet — the stub handlers raise `NotImplementedError` while noting the `NativeBackend` blocker. 16 CLI tests + all 1314 mordred-hermes tests green, ruff + `mypy --strict src` clean.

- [x] `hermes mordred keyvault init` (the Seed Phrase + Passphrase + PoW generation flow, network blackout assert → Seed display → offline/manual digest match) — **Phase 4 PR10 step-D completed**. `keyvault_cli.init_keyvault` + `TerminalSeedSurface`: re-init guard / double Passphrase prompt / BIP39 generation / seed-bound PoW / `prepare_generate` → `display_seed` → offline digest confirmation → `confirm_generate(*, backend=_SecKeyBackend())`. Also provisions the audit-log wrapping key (L465)
- [x] `hermes mordred keyvault list` (key IDs only, never displays key material) — landed in Phase 4 PR8. `keyvault_cli.list_keys` shows each key's cleartext id / on-disk hash / `created_at` from `meta.json`, without the verification digest (key material). An empty / uninitialized keyvault returns rc 0
- [x] `hermes mordred keyvault verify-digest` — landed in Phase 4 PR8. `keyvault_cli.verify_digest` shows each key's full 32-byte verification digest in hex (for offline cross-checking). An empty vault or an unreadable `digests/<hash>.commit` returns rc 1
- [x] `hermes mordred keyvault recover --blob <path>` — **Phase 4 PR10 step-C completed**. `keyvault_cli.recover`: reads the blob + prompts for Seed/Passphrase + pre-validates the BIP39 checksum + recomputes the seed-bound PoW + calls `api.import_backup(*, backend=_SecKeyBackend())`. Distinguishes `RecoveryDigestMismatch` / `BackupCorrupt` / `WrapError`
- [x] `hermes mordred audit decrypt --date YYYY-MM-DD` (requires Secure Enclave authorization) — **Phase 4 PR10 step-B completed**. `audit_cli.decrypt`: decrypts rotated `audit.log.<date>[.N][.gz]` files plus the current day's active log via `log_encryption.decrypt_log_file` → `unwrap_dek` → `backend.enclave_ecdh`, distinguishing denied prompt / missing wrapping key / corrupt file
- [x] `hermes mordred audit purge --before YYYY-MM-DD --yes` (manual deletion of pre-Phase-4 plaintext logs; see PATHS.md §Consumer CLI, PLAN.md §Audit-log encryption coupling) — landed in Phase 4 PR8. `audit_cli.purge` deletes rotated `audit.log.<date>[.N][.gz]` files strictly older than the cutoff date, never touches the active `audit.log`, ignores non-dated rotation files, and returns rc 2 on an invalid date

### 4.3 Tests (Phase 4)

- [x] `tests/test_digest.py`: fixed-vector tests for `top4(PoW)` extraction, matching the SPEC example — implemented in Phase 4 PR2 as `tests/test_keyvault_digest.py` (currently 20 tests passed). SPEC.md's fixed vector is the canonical regression anchor, covering XOR width / length confusion / constant-time compare
- [x] `tests/test_backup.py`: backup/recovery roundtrip with a mocked native binding — implemented in Phase 4 PR2 as `tests/test_keyvault_backup.py` (currently 43 tests) + `tests/test_keyvault_recovery.py` (currently 17 tests). The mocked-native-binding portion landed in Phase 4 PR3 as `tests/test_keyvault_wrap.py` (FakeBackend)
- [x] `tests/test_crypto.py`: AES-GCM encrypt/decrypt roundtrip + (GCM-layer) tag-verification failure — implemented in `tests/test_keyvault_crypto.py` (Phase 4 PR1 #21, covering AES-128/192/256 roundtrips, currently 16 tests). Secure Enclave wrap/unwrap failure is covered separately in Phase 4 PR3's `tests/test_keyvault_wrap.py`
- [x] Keyvault lifecycle tests (127 total): `tests/test_keyvault_{seed_display_handle,prepare_generate,confirm_generate,confirm_generate_deadline,generate}.py` cover `prepare_generate` / `confirm_generate` / `generate`, `SeedDisplayHandle` consume/wipe, and `keyvault.init_*` audit emission — landed in Phase 4 PR4 (PR #26, #28), split by responsibility 2026-07-28
- [x] `tests/test_keyvault_api_envelope.py` (currently 67 tests): MREN envelope wire-format encode/parse + cross-purpose replay defense — landed in Phase 4 PR4 (PR #26)
- [x] `tests/test_keyvault_api_storage.py` (currently 50 tests): managed storage's atomic write / `flock` / mode verification / meta roundtrip — landed in Phase 4 PR4 (PR #27)
- [x] `tests/test_keyvault_api_normalization.py` (currently 56 tests): the BIP39 NFKD / casefold / whitespace normalization split — landed in Phase 4 PR4 (PR #26)
- [x] `tests/test_keyvault_wrap.py` (currently 67 tests) + `tests/test_keyvault_native.py` (currently 11 tests): Secure Enclave wrap/unwrap (FakeBackend = software P-256) + the native capability probe — landed in Phase 4 PR3 (PR #25)
- [x] `tests/integration/test_keyvault_macos.py` (gated by `MORDRED_KEYVAULT_LIVE=1`, macOS arm64/T2 only) — **PR9 + PR10 step-F completed**: verifies real Secure Enclave wrapping-key generate + DEK wrap/unwrap roundtrip + capability probe + idempotent delete (fails, rather than skipping, when the gate is ON but no Enclave is present, SPEC HIGH-5). The AES-GCM (`api.encrypt`/`decrypt`) roundtrip was **added in Phase 4 PR10 step-F** (`test_encrypt_decrypt_roundtrip_through_real_enclave`, verifying generate → encrypt → decrypt via real Enclave authorization)
- [x] `tests/integration/test_network_fallback.py`: verifies that `network_fallback` determines blackout via the OS API when `mordred_network` is absent — **Phase 4 PR10 step-F completed**. Substitutes the `_import_network_api` seam with an ImportError, verifying the OS-API fallback's isolated/reachable/fail-closed paths + delegation when `mordred_network` is present
- [x] Cross-machine recovery test: export → off-by-one Passphrase → import_backup rejects → the correct entry succeeds → decrypt — implemented in Phase 4 PR4 step-E's `tests/test_keyvault_api_backup.py` (a cross-machine roundtrip using 2 FakeBackend devices + 2 home roots; wrong passphrase / wrong seed raises `RecoveryDigestMismatch`, a corrupt blob raises `BackupCorrupt`, and a digest mismatch is rejected before any Enclave key is generated)

### Acceptance gate (Phase 4)

- [x] Skill declaring `requires_keyvault: true` blocks install if keyvault not initialized — **completed 2026-05-16** (§4.1). Under strict policy, if the keyvault isn't initialized, `install_wrapper.run` raises `InstallBlocked` (`policy.strict.keyvault_uninitialized`), recording the audit entry first. Verified by `tests/test_install_wrapper.py::TestRequiresKeyvault` + `tests/test_policy.py::TestEvaluateInstallKeyvault`. lenient=warn / off=allow are covered at the same time
- [x] Keyvault-protected secret encrypts/decrypts through AES-GCM, DEK wrapped/unwrapped through Secure Enclave authorization — **PR10 completed**: `api.encrypt`/`decrypt` already landed in PR4; PR10 step-F's `test_keyvault_macos.py::test_encrypt_decrypt_roundtrip_through_real_enclave` (gated by `MORDRED_KEYVAULT_LIVE=1`) verifies the roundtrip via real Secure Enclave authorization
- [x] Backup → wipe → restore → decrypt roundtrip works — **PR4 step-E + PR10 step-C completed**: the api-level roundtrip is the cross-machine test in `test_keyvault_api_backup.py`; the CLI wiring is `keyvault_cli.recover` (`test_wizard_keyvault_cli.py::TestRecover::test_recover_roundtrip`)
- [x] Seed display always runs blackout check (api or fallback) first; refused on check failure — **PR7 + PR10 completed**: `display_seed` calls `network_fallback.resolve_blackout_assert` first and fail-closed aborts; the `keyvault init` CLI (`init_keyvault`) wires `display_seed` through the entire `BlackoutNotAsserted` path (`TestInit::test_blackout_failure_returns_1`)
- [x] `import_backup` does not complete unless recomputed digest equals embedded digest — **PR4 + PR10 completed**: verify-before-decrypt is in `recovery.import_backup`; on the CLI path, `keyvault_cli.recover` surfaces `RecoveryDigestMismatch` as rc 1 (`TestRecover::test_wrong_passphrase_returns_1`)
- [x] In `mordred_network`-absent envs, `keyvault init` still functions via OS API fallback — **PR5 + PR10 completed**: the OS-API fallback in `network_fallback.resolve_blackout_assert`; `tests/integration/test_network_fallback.py` simulates `mordred_network`'s absence and verifies the isolated/reachable/fail-closed paths
- [x] After Phase 4 lands, audit log is AES-GCM encrypted (test by failing decryption with `openssl`) — **PR6 + PR10 step-E completed**: in addition to `EncryptedWriter` / `decrypt_log_file` (`MRAL` v1), the `privacy_check.audit.make_audit_writer` factory selects `EncryptedWriter` once the keyvault is initialized (via `_runtime._load_state`). `test_audit_factory.py` verifies encrypted-when-initialized / fail-open-to-NDJSON-when-not

---

## Cross-cutting (ongoing through the operational phase)

- [ ] Changelog: add a 1-line entry to `### Changes` / `### Fixes` in each PR + `Thanks @<author>`
- [ ] **Decide on production wiring for `proxy_env.evaluate_library_compatibility`** (detected 2026-07-08; its 2 sibling items were resolved on 2026-07-13): `provider_transport_flagger.evaluate` and `paths/tor.py::circuit_status_health` were **wired up on 2026-07-13** (see the improvement-sweep backlog below) — evaluate is now called from the transport-compat gate in `hooks.on_session_start`, raising `MordredPathBringupFailed` on strict+abort, and circuit_status_health was promoted to `Runtime`'s default `tor_health` (with graceful self-degrade). What remains is `proxy_env.evaluate_library_compatibility` (zero callers): it takes the skill-declared list of HTTP libraries as input, but there's no runtime path to obtain that declaration, so where to wire it in is still undecided. Once the `origin_skill` payload (v2-H2) lands in v2, it could be resolved via skill metadata — until then, a judgment call is needed on whether to defer it or find a different input source

<!-- 2026-07-13 improvement-sweep code-review deferred backlog. All 6 items were implemented on 2026-07-13 (PR #42). See `.claude/reviews/local-review-improvement-sweep.md` §Deferred for details. -->
- [x] **Deep Tor liveness false idle-drop**: `circuit_status_health` now treats an authenticated empty reply or a reply with at least one non-terminal circuit as healthy because idle Tor builds circuits on demand. A known `FAILED`/`CLOSED`-only reply is unhealthy, while a syntactically valid future uppercase status is assumed non-terminal. Authentication failure, unreachable ControlPort, GETINFO failure, and malformed replies remain unhealthy. Runtime-worker regressions verify repeated idle probes never set `_dropped` or emit `network.path_dropped`, while terminal-only probes still do.
- [x] **fail-open** the Mullvad health probe in `paths/vpn.py::health` used a scope-less `wg show` and picked the most recent handshake across *all* WireGuard interfaces (`parse_handshake_age` returned the min) — if any non-Mullvad tunnel had a more recent handshake, it would misreport healthy even when Mullvad itself was dead, letting a strict kill-switch drop go undetected. **Fixed 2026-07-13**: switched to the daemon's own `mullvad status` Connected signal (the same one `wait_connected` uses, immune to being fooled by a sibling wg interface). `parse_handshake_age` remains in use by the generic-WireGuard provider, which scopes to its own interface
- [x] **fail-open** `keyvault/extension_sign.py::_audit_sink` discarded audit entries to `_log.debug` instead of the permanent log, so extension-mediated signing (`personal_sign` / `eth_signTypedData_v4` / `eth_sendTransaction`) and Secure-Enclave authorization (`unwrap_authorized` / `unwrap_denied`) left no trail. **Fixed 2026-07-13**: now does a best-effort append via `safe_audit_append` to the same encryption-capable audit.log writer used by network/llm_guard/privacy_check (`build_audit_writer`, lru_cache-memoized) (a signature is never dropped due to an audit failure)
- [x] **safe refactor** `keyvault/_env_write_guard.py::reseal_stray_env_if_present` did a runtime import of wizard via `from ..wizard import env_decrypt_cli`, inverting the intended `wizard → keyvault` dependency direction for roughly 150 call sites. **Fixed 2026-07-13**: moved the non-interactive reseal core down into a new `keyvault/_env_reseal.py::reseal_env`, which `_env_write_guard` now calls directly, while `wizard/env_decrypt_cli.reseal` becomes a thin wrapper (restoring the correct wizard → keyvault direction). Tests verify wizard is not imported, both statically and dynamically
- [x] **safe refactor** audit-log rotation / timestamp / retention logic was duplicated verbatim across `privacy_check/audit.py` / `keyvault/log_encryption.py` / `wizard/openclaw_migration.py`. **Fixed 2026-07-13**: extracted it into a pure-stdlib root module, `mordred_hermes/_log_rotation.py` (`utcnow_iso` / `today_utc_date` / `next_rotation_target` / `sweep_retention`), now shared by all 3 modules. `log_encryption._rotate`'s inline collision loop was replaced only after verifying byte-equivalence with `next_rotation_target`, and the monkeypatch seam was preserved
- [x] ~~(v2) monitor Hermes upstream hook **payload field shape** drift in CI~~ → **implemented 2026-06-12**: `tools/check_hook_payload_drift.py` (walks every upstream `invoke_hook("<name>", key=value, ...)` dispatch site using pure `ast`, no import/install required) + the consumed-field contract `tools/hook_payload_contract.json` + the `Check hook payload field drift` step in `upstream-check.yml` (on drift, lists the missing fields per site in the issue body). `tests/test_hook_payload_drift.py` enforces an exact match between the contract's keys and the `register_hook` calls, and this canary runs the same check against the vendored fork on every CI run
- [ ] (v2 consideration) if the `[hard-lock]` extra (vendored fork, Tier B) is introduced, reevaluate the conditions that trigger `mordred.degraded.disable_unprotected` (since the vendored fork requires a core-side `--unlock` flag, the plugin-side fallback would become redundant)
- [ ] Bump the version at release time — `python tools/bump_version.py <new>` updates `__about__.py` (canonical) + `docs/dev/VERSION` + all `plugin.yaml` files together (`0.1.0a0` → `0.1.0a1`/`0.1.0b0`/`0.1.0rc0` → `0.1.0` at v1 GA → `0.1.1` patch, etc. — all PEP 440 compliant). `tests/test_packaging_versions.py` guarantees agreement across the 7 surfaces and that stub < real in CI. Do not manually restore a static `version` to pyproject (the dynamic source is the sole canonical one)
- [x] Add a deprecation marker to the old `mordred-mvp-docs/` (OpenClaw-based) (a cleanup task after Phase E completes) — **completed 2026-05-17**: created `../../mordred/mordred-mvp-docs/README.md` (outside this repository, the same legacy docs tree as L3) and inserted a deprecation note at the top of the 12 `.md` files in that directory (CI/PATHS/PLAN/ROADMAP/SPEC/TODO's md+ja.md, and UPSTREAM.md). Since it's a separate tree, it doesn't show up in this PR's diff. Phase E (the upgrade Story 1.5) was already completed at L211
- [ ] (post-GA) reorganize `docs/` into topic-based subdirectories (`strategy/` `spec/` `ops/` `hermes/` `dev/`). New docs added before GA should be placed flat with an `HERMES_*.md` / `DEV_*.md` prefix, then `git mv`'d + have their cross-references rewritten in bulk in the migration PR. See `ROADMAP.md` v2-X3 for details
